/* AUTO-GENERATED by scripts/build-lessons.js. Do not edit by hand. */
/* Source: lessons/*.md rendered via assets/js/markdown.js. */
const LESSONS = {
  "m01-what-is-programming": "<h2>Why we're starting here</h2>\n<p>Before we touch any tool — VSCode, Docker, Claude, any of it — let's make sure we share the same mental model. If you don't, every single thing that comes next will feel like a confusing pile of jargon. Five minutes here will save you hours later.</p>\n<p>Also: a thing that bears repeating up front — <strong>there is no single \"right\" way to build software.</strong> The way we build is one way. It works for us. You'll see plenty of teams doing it differently, and most of them are also doing it right. Your job in this curriculum isn't to learn The Universal Method — it's to learn <strong>our</strong> method well enough to be a useful teammate.</p>\n<h2>What programming actually is</h2>\n<p>Programming is <strong>giving a computer a precise sequence of instructions.</strong> That's it.</p>\n<p>A computer cannot guess. It cannot infer. It will do exactly what you tell it, in the exact order, with no charitable reading of what you \"meant.\" That's both its weakness (frustrating when you typo a variable name) and its strength (totally predictable once you understand the rules).</p>\n<p>The instructions have to be written in something the computer understands. That something is a <strong>programming language.</strong></p>\n<h2>Language vs. framework vs. library vs. tool</h2>\n<p>These get used interchangeably in casual conversation and it makes everything confusing. Here's the cheat sheet:</p>\n<p><strong>Language</strong> — the grammar and vocabulary you use to write instructions. Examples: <strong>Python</strong>, <strong>JavaScript</strong>, <strong>Go</strong>, <strong>Rust</strong>. A language has rules (how you declare a variable, how you write a loop) and that's it. It doesn't know anything about websites or databases or AI.</p>\n<p><strong>Library</strong> — a pre-written bundle of code you can call when you need it. You decide when to use it. Example: in Python you might <code>import requests</code> to talk to the internet. Your code is in charge; the library waits to be called.</p>\n<p><strong>Framework</strong> — a pre-written skeleton of an <em>application</em> that you fill in. The framework is in charge of the overall shape; you plug your unique logic into the slots it provides. Examples: <strong>React</strong> (for websites), <strong>FastAPI</strong> (for backend APIs), <strong>Expo</strong> (for mobile apps).</p>\n<blockquote><p>The quick way to keep them straight: <strong>you call a library; a framework calls you.</strong></p></blockquote>\n<p><strong>Tool</strong> — anything that helps you build, but isn't part of your app's code. Examples: <strong>Docker</strong> (packages your app), <strong>VSCode</strong> (your editor), <strong>git</strong> (tracks changes to your code), <strong>Claude Code</strong> (an AI co-developer).</p>\n<p>So when you hear \"what's the stack?\" — what you'll get back is usually a mix of all four. Something like <em>\"Python, FastAPI, PostgreSQL, Docker, deployed on Cloud Run\"</em>. Now that mix should at least parse.</p>\n<h2>A concrete picture</h2>\n<p>Take <strong>CVstomize.com</strong>, one of the projects on Nolan's portfolio. Here's how those words map onto it:</p>\n<p><img src=\"assets/img/stack-diagram.svg\" alt=\"A worked example: CVstomize.com mapped onto the four categories — Language (JavaScript), Framework (React), Library (Material-UI), and Tools (GCP Cloud Run, Firebase Auth, Docker, Gemini 2.5).\" /></p>\n<ul><li><strong>Language:</strong> JavaScript (frontend), some backend code</li><li><strong>Framework:</strong> React (frontend)</li><li><strong>Library:</strong> Material-UI (pre-built UI pieces)</li><li><strong>Tool:</strong> GCP Cloud Run (where it actually runs), Firebase Auth (login)</li><li><strong>AI piece:</strong> Vertex AI / Gemini 2.5 (the \"conversational resume building\" engine)</li></ul>\n<p>One product. Five categories of stuff making it go. Don't memorize that list — just notice that every real app is a <em>combination</em> of those layers.</p>\n<h2>\"If you can think it, you can build it\"</h2>\n<p>You're going to hear this a lot. It's not motivational fluff — it's a real claim about how building has changed. The bottleneck used to be <strong>typing the syntax</strong>: you had to know exactly how a <code>for</code> loop is spelled in C, how to set up a HTTP server, how to format JSON. That's a lot of knowledge before you could build anything real.</p>\n<p>With Claude as your co-developer, almost all of that friction is gone. What's left is the part the AI <em>can't</em> do for you: <strong>knowing what you actually want to build, and being able to describe it clearly.</strong></p>\n<p>That's the skill we're really training in this curriculum. Everything else — Python syntax, Docker, MCPs — is just enough plumbing to make your ideas real.</p>\n<h2>Exercise: name the layers of an app you already use</h2>\n<p>5 minutes. The goal: make the four categories (language / framework / library / tool) stick by mapping them onto something real you already use.</p>\n<p>You'll need an AI helper — Claude, Gemini, ChatGPT, or Copilot — open in a browser tab. (Set that up in the <a href=\"lessons/02-ai-as-codev.md\" target=\"_blank\" rel=\"noopener\">next lesson</a> if you skipped ahead.)</p>\n<ol><li>Pick an app or website you actually use a lot. Spotify. Slack. Discord. Instagram. The Verge. Whatever.</li><li>Open your AI helper and paste in this prompt (swap in your app):</li></ol>\n<p>   &gt; <em>\"What programming language, framework, library, and tools does Spotify (the web player) most likely use? Give me one or two best-guesses per category, in plain English, in under 150 words.\"</em></p>\n<ol><li>Read the answer. Notice which category was easiest for the AI to be confident about (usually the framework) and which was fuzziest (often the libraries).</li><li>Now ask a follow-up: <em>\"What's one thing the AI couldn't actually know for sure, and how would I find out?\"</em></li></ol>\n<p>That's it. You've now done a real \"AI as search engine\" loop on a topic from this lesson — and seen what categories the four-bucket model actually maps onto in the wild.</p>\n<h2>Portfolio track: meet <code>my-tools</code></h2>\n<p>There's a running side project that grows alongside this curriculum. By the end of Module 11, you'll have a small public site — <code>&lt;your-username&gt;.github.io/my-tools/</code> — with one real tool you built on it, and the muscle to keep adding to it forever.</p>\n<p>Each module adds one small piece. Today, you don't add anything — you just know it's coming. The point: every install, every command, every habit in the next ten lessons lands on a real artifact you can point friends, recruiters, and future-you at.</p>\n<p>Keep an eye out for the <strong>Portfolio track</strong> section at the end of each lesson — that's where you add the next piece.</p>\n<h2>What to take away</h2>\n<ul><li>Computers do exactly what you tell them, no more, no less.</li><li>\"Language,\" \"framework,\" \"library,\" \"tool\" mean specific different things. Real apps mix all of them.</li><li>There's no one right way to build software. We're teaching you ours.</li><li>The new bottleneck isn't typing — it's clear thinking. That's what you're here to practice.</li></ul>\n<p>When you're ready, take the quiz to lock it in. Five questions, eighty percent to pass.</p>",
  "m02-ai-as-codev": "<h2>AI is the teammate, not a magic button</h2>\n<p>Most people meet AI as a chatbot — a thing in a browser tab you ask questions and get answers from. That's fine for many things, but it's not where the real leverage is.</p>\n<p>For us, an AI like Claude is a <strong>co-developer</strong>. That means it sits next to you (in your editor, in your terminal, in your project), it reads your files, runs your commands, and writes code that goes into the same repo your work goes into. You review what it produces just like you'd review a teammate's pull request. You give it feedback, it course-corrects, you keep going.</p>\n<p>This shifts the work. You stop spending most of your time typing syntax. You spend most of your time <strong>deciding what to do next</strong> and <strong>judging whether what just got produced is right.</strong> That's a different skill. Better news: it's a <em>more transferable</em> skill.</p>\n<h2>The big AI labs, at a glance</h2>\n<p>You should know the landscape — even if our team uses Claude for the heavy coding work. Here are the players that matter right now:</p>\n<table><thead><tr><th>Company</th><th>Model family</th><th>What they're known for</th></tr></thead><tbody><tr><td><strong>Anthropic</strong></td><td>Claude (Opus, Sonnet, Haiku)</td><td>Strong coding, careful tool use, good at long agentic tasks. Our pick for building software.</td></tr><tr><td><strong>OpenAI</strong></td><td>GPT / ChatGPT, o-series</td><td>Massive consumer reach, strong reasoning models, image generation, the broadest ecosystem.</td></tr><tr><td><strong>Google</strong></td><td>Gemini (Pro, Flash, Nano)</td><td>Deep integration with Google products (Docs, Gmail, Drive), strong multimodal, free tier on Google accounts.</td></tr><tr><td><strong>Microsoft</strong></td><td>Copilot (built on OpenAI + own models)</td><td>Wired into <em>everything</em> Microsoft — Windows, Office, Edge, GitHub, Teams. Hard to avoid if you live in that ecosystem.</td></tr><tr><td><strong>Meta</strong></td><td>Llama (open-weight)</td><td>You can download and run it yourself. Big in the open-source / self-hosted world.</td></tr><tr><td><strong>xAI</strong></td><td>Grok</td><td>Tied to X (Twitter). Different content posture than the others.</td></tr><tr><td><strong>DeepSeek / Mistral / Qwen / others</strong></td><td>various</td><td>Strong open and semi-open models. Useful when cost or self-hosting matters.</td></tr></tbody></table>\n<p>A few honest things about all of them:</p>\n<ul><li><strong>No single model is best at everything.</strong> A model that wins at coding might lose at creative writing, and vice versa. Benchmarks shift every few months.</li><li><strong>They all have weaknesses.</strong> Hallucinations (confidently wrong answers), training-data cutoffs, biases. None of them are oracles.</li><li><strong>They all have strengths.</strong> Picking one is about <em>fit for your workflow</em>, not bragging rights.</li></ul>\n<h2>Two AI helpers, not one</h2>\n<p>Here's how we actually use AI day-to-day, and this matters:</p>\n<ol><li><strong>Your coding co-developer</strong> — the one wired into your editor and terminal, editing your real files. For us, that's <strong>Claude (via Claude Code)</strong>. The rest of this curriculum sets that up.</li><li><strong>Your everyday helper</strong> — the chatbot you keep open in a browser tab or as a desktop app for <em>non-code</em> questions: \"explain this legal clause,\" \"summarize this 40-page PDF,\" \"rewrite this email.\" This can be a different tool than your coding one. Nolan himself uses <strong>Gemini</strong> for most everyday browser tasks, and Claude for coding. That's a totally normal split.</li></ol>\n<p>You're going to pick your everyday helper in this lesson's lab. The coding side gets set up later when we install Claude Code.</p>\n<h2>Picking your everyday helper</h2>\n<p>You've got four good options. None of them is wrong. Pick based on <em>where you already live</em>.</p>\n<h3>Claude Desktop (Anthropic)</h3>\n<ul><li><strong>Strengths:</strong> Same model that's great at coding. Best-in-class at careful long-form writing, summarization, document analysis. Strong file-attachment workflow (drop in a PDF, it reads it). Best at <em>following instructions exactly</em> without going off-script.</li><li><strong>Weaknesses:</strong> No native image generation. Smaller free tier than ChatGPT or Gemini. Less ecosystem integration than Google or Microsoft tools.</li><li><strong>Pick this if:</strong> You want one consistent assistant for both coding <em>and</em> everyday use, and you mostly work in PDFs / documents / text.</li></ul>\n<h3>Gemini (Google)</h3>\n<ul><li><strong>Strengths:</strong> Free and powerful tier with any Google account. Lives natively inside Gmail, Docs, Drive, Sheets — it sees your Google stuff if you let it. Strong multimodal (image, audio, video). Available everywhere on Android. <strong>Nolan uses Gemini as his daily browser helper</strong> for exactly these reasons.</li><li><strong>Weaknesses:</strong> Coding behavior is improving but isn't where Claude is for long agentic tasks. Privacy posture is complicated if you're sensitive about Google reading your data.</li><li><strong>Pick this if:</strong> You live in Gmail/Docs/Drive, want a strong free helper, or want the best mobile (Android) experience.</li></ul>\n<h3>ChatGPT (OpenAI)</h3>\n<ul><li><strong>Strengths:</strong> The biggest brand, the most plugins, native image generation (DALL·E), strong voice mode, broadest app ecosystem. If a third-party tool integrates <em>one</em> AI, it's usually this one.</li><li><strong>Weaknesses:</strong> Free tier is limited; the good models are paid. Can be more verbose / \"AI-voiced\" by default than Claude or Gemini.</li><li><strong>Pick this if:</strong> You want image generation, voice conversations, or the widest range of integrations and want to dabble in everything.</li></ul>\n<h3>Microsoft Copilot</h3>\n<ul><li><strong>Strengths:</strong> Built into Windows itself, Office (Word/Excel/PowerPoint/Outlook), Edge, and GitHub. If your work is in Microsoft tools, Copilot is <em>right there</em> — no separate app, no separate login, often free for personal use. Some versions can read your O365 documents directly.</li><li><strong>Weaknesses:</strong> Multiple flavors with different capabilities (Copilot, Copilot Pro, Microsoft 365 Copilot, GitHub Copilot) — easy to get confused which you have. Less \"pure chatbot\" focus than the others.</li><li><strong>Pick this if:</strong> You spend your day in Windows + Office + Outlook and want AI inside those apps without adopting anything new.</li></ul>\n<blockquote><p>Heads-up: there's also <strong>AI that controls your browser</strong> for you — tools like Anthropic's Computer Use, OpenAI's Operator, and browser-native agents (Comet, Arc Browse for me, Microsoft Edge Copilot Actions). You point them at a website and say \"book this for me\" or \"fill out this form across these tabs\" and they click through it themselves. Powerful, still rough around the edges. Worth knowing exists; not required for this course.</p></blockquote>\n<h2>Why we picked Claude <em>for coding</em></h2>\n<p>A few specific reasons our team defaults to Claude for the actual building of software:</p>\n<ol><li><strong>Coding behavior.</strong> It tends to make smaller, more surgical edits, asks fewer dumb questions, and reads your code carefully before changing it. That matters a lot when it's editing a real codebase.</li><li><strong>Tool use.</strong> Claude is good at using <em>tools</em> — running commands, reading files, calling APIs — and stopping to think before doing something destructive. (See [[m08-mcps-and-addons]].)</li><li><strong>Long agentic loops.</strong> It can run for many steps in a row without going off the rails. That's how we get features built in an afternoon that would take a team a week.</li><li><strong>Claude Code.</strong> Anthropic's CLI tool <em>is</em> the workflow we use. It lives in your terminal, reads your repo, can edit files, run tests, and lean on extensions and MCPs. Nolan has even built and published Claude Code add-ons (<strong>Daemonstrate</strong>, <strong>TTYL</strong> — both on his portfolio).</li></ol>\n<p>If at some point Gemini or GPT or something newer is clearly better for what we're doing, we'll re-evaluate. But for now, for <em>coding</em>: Claude.</p>\n<h2>\"If you can think it, you can build it\" — again, and on purpose</h2>\n<p>You're going to hear this phrase a lot in this curriculum. It's not a slogan, it's the operating principle.</p>\n<p>With AI as a co-developer:</p>\n<ul><li>The <strong>idea</strong> is the bottleneck.</li><li>The <strong>clarity of what you want</strong> is the bottleneck.</li><li>The <strong>judgment</strong> about whether the result is right is the bottleneck.</li></ul>\n<p>The typing is not the bottleneck anymore. That's the whole point.</p>\n<p>Look at Nolan's portfolio (https://wyofalcon.github.io/Portfolio/). Projects like <strong>NoteXT</strong>, <strong>CVstomize</strong>, <strong>HuminLoop</strong>, <strong>(Ai)nsworth</strong> — those are the kinds of things small teams used to take many months to ship. He built and shipped them as one person leveraging Claude. Not because he's secretly a 50-person engineering team. Because the bottleneck moved.</p>\n<h2>Lab: pick your AI helper and put it to work</h2>\n<p>This lab takes about 10 minutes. By the end you'll have a helper installed (or bookmarked), you'll have used it once, and you'll have your first instinct for when to reach for it.</p>\n<p>You'll find the setup checklist for this right below the lesson — that's what gates the quiz. Work through it as you read.</p>\n<h3>Step 1 — Pick one</h3>\n<p>Look at the four options above. Don't agonize. Pick the one that fits where you already are:</p>\n<ul><li>Already live in Google? → <strong>Gemini.</strong></li><li>Already live in Microsoft Office / Windows? → <strong>Copilot.</strong></li><li>Want the same brand as the coding side of the course? → <strong>Claude Desktop.</strong></li><li>Want image generation, voice, the widest integrations? → <strong>ChatGPT.</strong></li></ul>\n<p>You can switch later. The goal is to have <em>one</em> you reach for by default so you stop second-guessing every time.</p>\n<h3>Step 2 — Install or sign in</h3>\n<ul><li><strong>Claude Desktop:</strong> download from claude.ai (top-right \"Get the desktop app\") or use claude.ai in your browser. Free tier is solid.</li><li><strong>Gemini:</strong> go to gemini.google.com and sign in with any Google account. There's also a Gemini app on Android and iOS. If you're on Android, the Assistant is being replaced by Gemini — it might already be on your phone.</li><li><strong>ChatGPT:</strong> chatgpt.com, sign up free. Desktop apps for Mac/Windows are at openai.com/chatgpt/download.</li><li><strong>Copilot:</strong> on Windows 11 it's already there — press the Copilot key on your keyboard (or <code>Win + C</code>). Also at copilot.microsoft.com. For Office, it shows up inside Word/Excel/Outlook if you have the right subscription.</li></ul>\n<h3>Step 3 — Try a real prompt</h3>\n<p>Don't just say \"hi\" and quit. Pick a real thing you actually need to do today and try it. Examples:</p>\n<ul><li>\"Summarize this PDF into 5 bullet points.\" (Attach a real PDF.)</li><li>\"Rewrite this email so it's two sentences shorter and a bit warmer.\" (Paste a draft.)</li><li>\"I'm trying to learn what WSL does — explain it like I'm new to dev, in under 100 words.\"</li><li>\"What's the difference between Python and JavaScript? I'm trying to decide what to learn first.\"</li></ul>\n<p>Notice how the answer feels. Is it specific? Did it follow your instructions? That's the muscle you're building — <em>judging the output</em>, not just consuming it.</p>\n<h3>Step 4 — Bookmark it / pin it</h3>\n<p>Pin the tab. Add the app to your taskbar. Put it on your phone. Friction kills habits — if your helper is one click away, you'll use it. If it's three clicks away, you won't.</p>\n<blockquote><p>A reminder that bears repeating: the helper you picked is for <strong>everyday browser/document work.</strong> When we get to coding, we'll install <strong>Claude Code</strong> in your terminal as the dev-side co-developer. It's normal to use two different AIs for two different jobs.</p></blockquote>\n<h2>Portfolio track</h2>\n<p>You just picked an everyday helper. Use it once, right now, on the side project.</p>\n<p>Open the helper and paste:</p>\n<blockquote><p><em>\"I'm going to build a small public site called <code>my-tools</code> — a portfolio of tiny tools I build for myself. As I go through a 1–2 hour intro course, I'll add one tool at a time. Help me draft a single-sentence pitch for it, in plain English, no marketing voice.\"</em></p></blockquote>\n<p>Save the answer to a scratch note. You'll drop it onto the live site in Module 4.</p>\n<h2>What to take away</h2>\n<ul><li>AI is your co-developer — not a chat toy and not a magic button.</li><li>Several big AI labs exist. They each have strengths. None is best at everything.</li><li>Most people end up with <strong>two AI helpers</strong>: one for everyday browser work, one wired into their coding environment. They don't have to be the same brand.</li><li>For everyday use, <strong>pick the one that fits where you already work</strong> (Gemini for Google folks, Copilot for Microsoft folks, Claude for documents/text-heavy work, ChatGPT for image/voice/breadth).</li><li>For coding, we picked <strong>Claude</strong> because of how it behaves as a coding partner and tool user, not because the others are bad.</li><li>The real skill you're building is <strong>clear thinking + good judgment</strong>. The AI handles the typing.</li></ul>\n<p>Next module: WSL and the terminal — the foundation your dev environment will live on. (And once you have your helper picked, lean on it every time something here trips you up. That's the workflow from now on.)</p>",
  "m03-wsl-and-terminal": "<h2>Why Linux, even on Windows</h2>\n<p>Here's the awkward truth about modern software: almost every server in the world runs Linux, almost every dev tool assumes Linux, and almost every tutorial you'll read shows Linux commands. If you're on Windows (Nolan is), that's a problem — until WSL.</p>\n<p><strong>WSL (Windows Subsystem for Linux)</strong> is Microsoft's official way to run a real Linux environment <em>inside</em> Windows. Not a virtual machine you boot into. Not a dual-boot setup where you pick an OS at startup. A real Linux kernel running side-by-side with Windows, with its own filesystem, its own package manager, its own everything — and you can pop in and out of it from a normal Windows terminal.</p>\n<p>You install it once (<code>wsl --install</code> in PowerShell, pick Ubuntu) and from then on, when we say \"open a terminal,\" we mean <em>open your WSL terminal</em>. That's where your code lives, where Docker runs, where Claude Code runs, where everything happens.</p>\n<blockquote><p>The quick way to remember it: <strong>Windows is your desktop. WSL is your workshop.</strong></p></blockquote>\n<p><img src=\"assets/img/wsl-diagram.svg\" alt=\"A Windows window with a WSL Ubuntu terminal running inside it. The terminal shows a typical morning: cd into a project, ls to see what's there, code . to open it in VSCode.\" /></p>\n<h2>What \"the terminal\" actually is</h2>\n<p>A terminal is a window where you type commands instead of clicking buttons. That's it. No mystique. Every command you type runs a program, and that program prints something back (or doesn't, if it succeeded silently — Unix tools are quiet on purpose).</p>\n<p>If you've only ever used graphical apps, this feels backwards at first. Stick with it for a day and it flips: the terminal is <em>faster</em> than clicking for almost anything a developer does, because you can chain commands, repeat them, and script them.</p>\n<h2>The commands you'll actually use on day one</h2>\n<p>You do not need to learn 200 commands. You need about seven. Here they are, with what they do:</p>\n<pre><code data-lang=\"bash\">pwd          # \"print working directory\" — where am I right now?\nls           # \"list\" — what files/folders are in here?\nls -la       # same, but show hidden files and details\ncd folder    # \"change directory\" — go into a folder\ncd ..        # go up one folder\ncd ~         # go to your home folder (your default workspace)\nmkdir name   # \"make directory\" — create a new folder\ncat file.txt # print the contents of a file to the terminal\nclear        # wipe the screen (your history is still there, just scrolled off)\ncode .       # open the current folder in VSCode (more on this next lesson)</code></pre>\n<p>That's enough to navigate any project, peek at files, and open things in your editor. Everything else you'll pick up as you need it — or ask Claude.</p>\n<h2>Where your files live: WSL vs. Windows</h2>\n<p>This trips people up, so let's nail it down.</p>\n<p>WSL has its own filesystem. Your \"home\" in WSL is <code>~</code> (which expands to something like <code>/home/yourname</code>). <strong>This is where your projects should live.</strong> Code in <code>~</code> runs fast, plays nicely with Docker, and behaves the way every Linux tool expects.</p>\n<p>Your Windows drives are still reachable from WSL — they're mounted under <code>/mnt/</code>. So your Windows <code>C:\\Users\\You\\Documents</code> shows up at <code>/mnt/c/Users/You/Documents</code>. You <em>can</em> work there. You shouldn't. It's much slower (crossing the Windows/Linux filesystem boundary on every read), and some tools misbehave.</p>\n<blockquote><p>The rule: <strong>code lives in <code>~</code>. <code>/mnt/c</code> is just a bridge for occasionally grabbing a file.</strong></p></blockquote>\n<p>From the other direction, you can browse WSL files from Windows File Explorer — type <code>\\\\wsl$\\</code> in the address bar and you'll see your distros. Handy for dragging files in. But day-to-day, stay in the terminal.</p>\n<h2>A quick tour</h2>\n<p>A typical morning for Nolan looks roughly like this:</p>\n<pre><code data-lang=\"bash\">cd ~/projects/some-project # jump into the project\nls                         # see what's there\ncode .                     # open it in VSCode (connected to WSL)</code></pre>\n<p>Three commands. You're now in your project, in your editor, in a real Linux environment. From here Claude Code, Docker, Python — all of it — is one step away. We'll layer those on in the next lessons.</p>\n<h2>Lab: get on the terminal</h2>\n<p>Reading about the terminal is one thing — using it is another. This lab takes about 10 minutes and gets the commands above into your fingers.</p>\n<p>You'll need a working WSL terminal to do it. If you haven't installed WSL yet, do that first:</p>\n<h3>Step 0 — Install WSL (Windows only — skip if you're already on Mac/Linux or WSL is set up)</h3>\n<ol><li>Open <strong>PowerShell as Administrator</strong> (right-click the Start menu → \"Terminal (Admin)\" or \"PowerShell (Admin)\").</li><li>Run:</li></ol>\n<p>   ``<code>powershell    wsl --install    </code>``</p>\n<ol><li>Reboot if it asks.</li><li>After reboot, a window opens asking you to set a <strong>Linux username and password</strong>. Pick something memorable — you'll type the password occasionally for admin (<code>sudo</code>) commands. It is <em>not</em> the same as your Windows password.</li><li>Confirm it worked:</li></ol>\n<p>   ``<code>powershell    wsl --version    </code>`<code>    You should see a version number. From now on, when we say \"open a terminal,\" open this one — search for \"Ubuntu\" in your Start menu, or just type </code>wsl` in any Windows terminal.</p>\n<blockquote><p>Hit a wall during install? This is the perfect moment to practice asking Claude (or Google) about the <em>exact</em> error message. Don't paraphrase — copy/paste it.</p></blockquote>\n<h3>Step 1 — Where am I?</h3>\n<p>Open your WSL terminal. The prompt looks something like <code>you@laptop:~$</code>. The <code>~</code> is shorthand for your home folder. Try:</p>\n<pre><code data-lang=\"bash\">pwd</code></pre>\n<p>You'll see something like <code>/home/you</code>. That <code>/home/you</code> is the <em>real</em> path; <code>~</code> is just a shortcut. Now list what's in there:</p>\n<pre><code data-lang=\"bash\">ls\nls -la</code></pre>\n<p>The first <code>ls</code> probably shows almost nothing (fresh WSL installs are empty). <code>ls -la</code> shows the <em>hidden</em> files too — anything starting with <code>.</code> is hidden by default. You'll see things like <code>.bashrc</code> and <code>.profile</code>. Those are your shell's config files. Don't touch them yet — just notice they exist.</p>\n<h3>Step 2 — Build a little file tree</h3>\n<p>Let's make a folder for your projects and poke around. Type these one at a time:</p>\n<pre><code data-lang=\"bash\">cd ~                       # make sure you're home\nmkdir projects             # create a \"projects\" folder\ncd projects                # go into it\npwd                        # confirm — should print /home/yourname/projects\nmkdir hello-terminal       # create a project folder\ncd hello-terminal\necho \"Hello from the terminal\" &gt; greeting.txt\nls\ncat greeting.txt</code></pre>\n<p>What just happened: you made two nested folders, created a text file with the <code>echo &gt; file</code> trick, listed the folder to confirm it's there, and printed the file's contents. You just did with seven commands what would have been a dozen mouse clicks in File Explorer.</p>\n<h3>Step 3 — Climb the tree</h3>\n<p>The filesystem is a tree. Your home (<code>/home/yourname</code>) lives inside <code>/home</code>, which lives inside <code>/</code> (the root of everything). Let's walk up it:</p>\n<pre><code data-lang=\"bash\">pwd            # /home/yourname/projects/hello-terminal\ncd ..          # up one level → /home/yourname/projects\npwd\ncd ..          # up again → /home/yourname\npwd\ncd ..          # → /home\nls             # you'll see your own user folder, and maybe others\ncd /           # jump all the way to the root\nls             # this is the whole Linux filesystem at a glance</code></pre>\n<p>You're now looking at folders like <code>bin</code>, <code>etc</code>, <code>home</code>, <code>usr</code>, <code>var</code>, <code>mnt</code>. A quick translation:</p>\n<ul><li><code>home/</code> — where users live (you're under here)</li><li><code>bin/</code>, <code>usr/bin/</code> — installed programs</li><li><code>etc/</code> — system config files</li><li><code>var/</code> — logs and changing data</li><li><code>mnt/</code> — <em>mounted</em> drives. This is where your Windows C: drive shows up</li></ul>\n<p>Peek at the Windows bridge:</p>\n<pre><code data-lang=\"bash\">ls /mnt/c           # you should see your Windows drive contents</code></pre>\n<p>That's your <code>C:\\</code> from inside Linux. Cool — but remember the rule from earlier: <strong>don't work there</strong>. It's slow, and tools misbehave across the boundary.</p>\n<h3>Step 4 — Back home, the fast way</h3>\n<pre><code data-lang=\"bash\">cd ~                       # straight back to your home\ncd projects/hello-terminal # jump multiple levels in one go\npwd</code></pre>\n<p>You don't have to climb one level at a time. <code>cd</code> takes a whole path.</p>\n<h3>Step 5 — Clean up (optional)</h3>\n<p>If you want to delete the practice folder:</p>\n<pre><code data-lang=\"bash\">cd ~\nrm -r projects/hello-terminal</code></pre>\n<p><code>rm -r</code> removes a folder and everything inside it. <strong>There is no trash bin</strong> — files deleted in the terminal are gone. Be slow and deliberate the first few times you use <code>rm</code>.</p>\n<h3>What you should feel after this lab</h3>\n<ul><li>A little bit of \"oh, that's it?\" The terminal stops feeling like wizardry.</li><li>Comfort with <code>pwd</code>, <code>ls</code>, <code>cd</code>, <code>mkdir</code>, <code>cat</code>, <code>echo &gt;</code>, <code>rm -r</code> — that's most of day-to-day navigation.</li><li>A clear mental picture: home is <code>~</code>, root is <code>/</code>, Windows is mounted at <code>/mnt/c</code>, and your code is going to live under <code>~/projects/</code>.</li></ul>\n<p>If anything went sideways — a command did nothing, an error message you didn't expect — copy/paste the exact output to Claude and ask \"what does this mean?\" That's the workflow from here on.</p>\n<h2>Portfolio track</h2>\n<p>Now that you can navigate WSL, create the folder your portfolio site will live in:</p>\n<pre><code data-lang=\"bash\">cd ~\nmkdir -p projects/my-tools\ncd projects/my-tools\necho \"# my-tools\" &gt; README.md\nls\ncat README.md</code></pre>\n<p>That's it for today. An empty folder with a README. We'll turn it into a real page in the next lesson.</p>\n<h2>What to take away</h2>\n<ul><li>WSL gives you a real Linux environment on Windows. No dual-boot, no VM hassle.</li><li>The terminal is where the work happens. Seven commands cover 90% of day-one navigation.</li><li>The filesystem is a tree rooted at <code>/</code>. Your home is <code>~</code>. Windows drives are bridged under <code>/mnt/</code>.</li><li>Keep your projects in your WSL home (<code>~</code>). Treat <code>/mnt/c</code> as a bridge, not a workspace.</li><li><code>code .</code> is the magic command that connects your terminal world to your editor world.</li></ul>\n<p>Next up: VSCode, and why it's the editor that ties this whole stack together.</p>",
  "m04-ide-and-vscode": "<h2>Editor vs. IDE</h2>\n<p>A <strong>text editor</strong> lets you edit text. Notepad is a text editor. So is the thing your phone uses for notes. It can open a file, let you change it, and save it. That's the whole job.</p>\n<p>An <strong>IDE</strong> — Integrated Development Environment — is a text editor that knows about <em>code</em>. It understands the language you're writing, highlights syntax, autocompletes as you type, jumps you to where a function is defined, runs your tests, talks to git, talks to a debugger, talks to a container, and ties a dozen other dev tools into one window. The \"integrated\" is the whole point: instead of juggling six tools, you have one.</p>\n<p>You can build software in a plain editor. People do. But once you've felt a real IDE catch a typo before you save, suggest the right import, and let you click-through to read a library's source code, going back feels like writing with mittens on.</p>\n<h2>Why VSCode</h2>\n<p>There are several good IDEs. We use <strong>Visual Studio Code</strong> (just \"VSCode\") for a few specific reasons:</p>\n<ol><li><strong>Free, and from Microsoft.</strong> Funded, maintained, and not going anywhere.</li><li><strong>Fast and light.</strong> It opens instantly. Heavier IDEs (looking at you, IntelliJ family) can take 30 seconds to launch. VSCode is ready before your coffee is.</li><li><strong>Extensions for everything.</strong> There's a marketplace with thousands of plugins. Whatever language or tool you're using, someone wrote a good extension for it.</li><li><strong>First-class WSL and Dev Containers support.</strong> This one matters a lot for our stack. VSCode can connect <em>into</em> WSL or <em>into</em> a running container and edit files there as if they were local. No other editor does this as smoothly.</li><li><strong>Franny and I both use it.</strong> Less switching cost when we're pair-debugging — same shortcuts, same layout, same extensions.</li></ol>\n<blockquote><p>Quick way to remember it: <strong>VSCode is the cockpit. Everything else plugs into it.</strong></p></blockquote>\n<p><img src=\"assets/img/vscode-ui.svg\" alt=\"Anatomy of the VSCode window: activity bar on the left, file explorer, editor with a Python file open, integrated terminal at the bottom, and the WSL: Ubuntu indicator in the bottom-left status bar.\" /></p>\n<h2>The must-have extensions for our workflow</h2>\n<p>When you first install VSCode, it's a blank canvas. The extensions below turn it into the cockpit we actually use. Install them from the Extensions tab (the square icon in the left sidebar) or via the command palette.</p>\n<ul><li><strong>WSL</strong> (by Microsoft) — lets VSCode connect into your WSL environment. After installing, when you run <code>code .</code> inside WSL, this is what makes it Just Work.</li><li><strong>Dev Containers</strong> (by Microsoft) — lets VSCode open a project <em>inside</em> a Docker container. This is how we get a guaranteed-identical dev environment per project. (Whole next lesson on this.)</li><li><strong>Python</strong> (by Microsoft) — syntax highlighting, autocomplete, debugger, test runner. Non-negotiable if you're touching Python.</li><li><strong>Claude Code</strong> — if you see an official Claude extension in the marketplace, install it. It hooks Claude Code into the editor UI so you can review diffs, accept changes, and chat with Claude without leaving VSCode. (We also run Claude Code as a CLI in the terminal — having both is the move.)</li></ul>\n<p>That's the core four. You'll pick up others as you need them — Docker, GitLens, Prettier, language-specific ones — but don't over-install on day one. Extensions you don't use are just noise.</p>\n<h2>The <code>code .</code> moment</h2>\n<p>Here's the workflow that ties it all together. From your WSL terminal:</p>\n<pre><code data-lang=\"bash\">cd ~/projects/GenAI101\ncode .</code></pre>\n<p>What just happened:</p>\n<ol><li>You navigated to your project folder in WSL.</li><li><code>code .</code> launched VSCode on Windows...</li><li>...but told it to connect <em>back into WSL</em> and open the current folder from there.</li></ol>\n<p>Look at the bottom-left corner of VSCode after it opens. You'll see something like <code>WSL: Ubuntu</code>. That's the indicator that you're editing files inside Linux, not on Windows. Every terminal you open inside VSCode (Ctrl+`) will be a WSL terminal. Every command you run will be a Linux command. Your editor is now a window into the Linux machine living inside your Windows machine.</p>\n<p>This is the setup. Once it clicks, everything else stops feeling weird.</p>\n<h2>Lab: get the cockpit running</h2>\n<p>10 minutes. By the end you'll have VSCode talking to WSL, with the right extensions, on a real folder.</p>\n<p>The setup checklist below the lesson covers the installs. This lab is what you should actually <em>do</em> with them.</p>\n<h3>Step 1 — Install + extensions (use the checklist)</h3>\n<p>Work through the setup checklist below to install VSCode and the <strong>WSL</strong> + <strong>Dev Containers</strong> extensions. Don't try to memorize what each one does — install them, then come back here.</p>\n<h3>Step 2 — Open a real folder from inside WSL</h3>\n<p>Open a WSL terminal and run:</p>\n<pre><code data-lang=\"bash\">cd ~\nmkdir -p projects/lab-vscode\ncd projects/lab-vscode\necho \"# Lab: VSCode\" &gt; README.md\ncode .</code></pre>\n<p>VSCode opens. Look at the <strong>bottom-left corner of the window.</strong> You should see something like <code>WSL: Ubuntu</code> in a colored badge. If you do — your editor is now living inside Linux. If you don't, the WSL extension probably needs a reload (<code>Ctrl+Shift+P</code> → \"Reload Window\").</p>\n<h3>Step 3 — Open the integrated terminal</h3>\n<p>Press `<code> Ctrl+</code> `<code> (control + the backtick key, top-left of your keyboard). A terminal opens at the bottom of VSCode. Run </code>pwd<code> — it should print </code>/home/yourname/projects/lab-vscode`. That's a WSL shell, inside VSCode, in your project. This is the daily setup.</p>\n<h3>Step 4 — Make an edit, save, see the file</h3>\n<p>In VSCode's file explorer (top-left, file icon), click <code>README.md</code>. Add a line. Save (<code>Ctrl+S</code>). Back in the terminal, run <code>cat README.md</code> — your edit is there. You just edited a Linux file from a Windows editor without thinking about it. That's the whole magic.</p>\n<h3>Step 5 — Ask your AI helper one VSCode-specific thing</h3>\n<p>Open your everyday helper (the one you picked in <a href=\"lessons/02-ai-as-codev.md\" target=\"_blank\" rel=\"noopener\">Module 2</a>) and ask it something like:</p>\n<blockquote><p><em>\"I'm new to VSCode. What are 3 keyboard shortcuts I'll use 50 times a day?\"</em></p></blockquote>\n<p>Try the ones it gives you. (Hint: <code>Ctrl+P</code> is the one Nolan uses most.)</p>\n<h2>Portfolio track</h2>\n<p>You've got an editor connected to WSL. Time to put the first page on the site.</p>\n<pre><code data-lang=\"bash\">cd ~/projects/my-tools\ncode .</code></pre>\n<p>In VSCode, create <code>index.html</code> with the bare bones:</p>\n<pre><code data-lang=\"html\">&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;\n  &lt;meta charset=\"utf-8\" /&gt;\n  &lt;title&gt;My Tools&lt;/title&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;My Tools&lt;/h1&gt;\n  &lt;p&gt;Small tools I built for myself.&lt;/p&gt;\n  &lt;div id=\"tools\"&gt;&lt;/div&gt;\n&lt;/body&gt;\n&lt;/html&gt;</code></pre>\n<p>Drop the tagline you got from your AI helper in Module 2 into the <code>&lt;p&gt;</code> if you like it. Then serve the folder:</p>\n<pre><code data-lang=\"bash\">python3 -m http.server 8000</code></pre>\n<p>Open <code>http://localhost:8000</code> in your browser. That's your portfolio site, live on your laptop. Empty so far — that's the whole point. The rest of the course fills it in.</p>\n<h2>What to take away</h2>\n<ul><li>An IDE is a text editor that understands your code and integrates your dev tools. VSCode is our pick.</li><li>Install four extensions to start: <strong>WSL</strong>, <strong>Dev Containers</strong>, <strong>Python</strong>, and the <strong>Claude</strong> extension if available.</li><li><code>code .</code> from a WSL terminal opens that folder in VSCode connected to WSL. Check the bottom-left for <code>WSL: Ubuntu</code> to confirm.</li><li>Franny and I both run this exact setup, which makes pair-debugging painless.</li></ul>\n<p>Next: Docker and devcontainers — how we make sure your machine and my machine run code the same way.</p>",
  "m05-docker-devcontainers": "<h2>The \"works on my machine\" problem</h2>\n<p>Here's a story every developer has lived through. You write some code. It runs perfectly. You hand it to a teammate. It blows up on their machine. You spend two hours discovering they're on a different OS, with a different Python version, with a missing system library, and the file paths use backslashes instead of forward slashes.</p>\n<p>That's the \"works on my machine\" problem. For decades it was just... the cost of doing business. Then containers happened.</p>\n<h2>What a container is</h2>\n<p>A <strong>container</strong> is a lightweight, isolated environment that includes everything your app needs to run — the right operating system files, the right language version, the right libraries, the right tools — packaged together and reproducible.</p>\n<p>Think of it like a sealed lunchbox. Inside the box: Python 3.11, the exact libraries your app needs, the right config files, your code. The lunchbox runs the same on Nolan's laptop, your laptop, a Cloud Run server in some Google datacenter, and a Raspberry Pi on someone's desk. Same box, same contents, same behavior.</p>\n<p>It's not a virtual machine. A VM emulates a whole computer (slow, heavy, gigabytes of overhead). A container shares the host's kernel and just isolates the file system and processes. Containers start in <em>seconds</em> and weigh megabytes, not gigabytes.</p>\n<p><img src=\"assets/img/container-vs-vm.svg\" alt=\"Side-by-side comparison: a VM stacks a Hypervisor and a full Guest OS under your app (heavy, slow), while a container shares the host kernel through a runtime like Docker (light, fast). Below: the clone → code . → Reopen in Container flow.\" /></p>\n<h2>What Docker is</h2>\n<p><strong>Docker</strong> is the tool that builds, runs, and manages containers. It's not the only one — but it's the one everyone uses, and the one our workflow assumes. When someone says \"containerize your app,\" they almost always mean \"write a Dockerfile so Docker can package it.\"</p>\n<p>On Windows + WSL, Docker Desktop is the install. Once it's running, the <code>docker</code> command works from inside your WSL terminal as if Docker were native Linux. You usually won't type <code>docker</code> commands directly day-to-day — VSCode does it for you — but it's good to know what's under the hood.</p>\n<blockquote><p>Quick way to remember it: <strong>A container is the lunchbox. Docker is the tool that packs it and serves it.</strong></p></blockquote>\n<h2>What a devcontainer is specifically</h2>\n<p>A <strong>devcontainer</strong> is a container set up for <em>development</em>, not for production. Same Docker tech underneath, different purpose.</p>\n<p>A production container is sealed and minimal: just your app, just what it needs to run, no extra weight. A devcontainer is generous: it includes your app's runtime <em>plus</em> the tools you write code with — git, your linter, your test runner, your debugger, your shell, sometimes even VSCode extensions pre-installed.</p>\n<p>Devcontainers are configured by a single file in your repo:</p>\n<pre><code>your-project/\n├── .devcontainer/\n│   └── devcontainer.json\n├── src/\n└── README.md</code></pre>\n<p>That <code>devcontainer.json</code> describes which base image to use, which tools to install, which VSCode extensions to load, and how to start the environment. Commit it to git. Now anyone who clones the repo gets the same dev environment you have — no setup instructions, no \"did you install Python 3.11?\", no \"what version of Node?\".</p>\n<h2>The magic moment</h2>\n<p>Here's the workflow that, once you've done it once, changes how you think about onboarding to a project:</p>\n<pre><code data-lang=\"bash\">git clone https://github.com/some/project.git\ncd project\ncode .</code></pre>\n<p>VSCode opens, sees the <code>.devcontainer/</code> folder, and prompts you: <strong>\"Reopen in Container?\"</strong> You click yes. Docker builds the image (first time only, a few minutes — cached after that). VSCode reconnects, and now you're editing inside a container that has the exact runtime, exact tools, exact extensions the project needs.</p>\n<p>No more \"install these 14 things first.\" No more \"make sure you're on this version of that.\" You're ready to run, test, and ship code in minutes.</p>\n<p>This is also how Nolan and Franny stay in sync on shared projects. Same <code>devcontainer.json</code> in the repo, same environment on both laptops, even though our host setups are different.</p>\n<h2>You get one to play with</h2>\n<p>This curriculum ships with its own <code>.devcontainer/</code> folder. After this lesson, you can clone the GenAI101 repo, open it in VSCode, reopen in container, and you'll be inside a sandbox you can experiment in safely. Break things. It's a container — throw it away and rebuild in 30 seconds.</p>\n<p>That experimentation is the whole point. You'll never really <em>get</em> devcontainers from reading about them. You get them the first time you delete something accidentally and realize it doesn't matter, because the environment is just a file.</p>\n<h2>Lab: open this curriculum in its own devcontainer</h2>\n<p>10 minutes. The best way to <em>get</em> devcontainers is to use one — and this curriculum ships with its own, on purpose.</p>\n<p>The setup checklist below makes sure Docker is installed and talking to WSL. This lab takes the next step: actually opening a project in a container.</p>\n<h3>Step 1 — Get the curriculum on your machine</h3>\n<p>If you haven't already, clone or download this repo into your WSL home. From a WSL terminal:</p>\n<pre><code data-lang=\"bash\">cd ~\nmkdir -p projects\ncd projects\n# If Nolan gave you a git URL:\ngit clone &lt;the-url-Nolan-sent-you&gt; GenAI101\n# Or if you got it as a folder, just copy it into ~/projects/GenAI101.\ncd GenAI101\ncode .</code></pre>\n<h3>Step 2 — Reopen in container</h3>\n<p>After VSCode opens, you should see a popup in the bottom-right:</p>\n<blockquote><p><strong>\"Folder contains a Dev Container configuration file. Reopen folder to develop in a container.\"</strong></p></blockquote>\n<p>Click <strong>Reopen in Container</strong>. (If you missed the popup: <code>Ctrl+Shift+P</code> → \"Dev Containers: Reopen in Container\".)</p>\n<p>First time, Docker will build the image. This takes a couple minutes. Subsequent opens are fast — Docker caches the build.</p>\n<h3>Step 3 — Notice you're in a different world</h3>\n<p>When it finishes, the bottom-left status bar changes from <code>WSL: Ubuntu</code> to something like <code>Dev Container: GenAI101</code>. You're now editing inside the container, not directly on WSL. Open the terminal (`<code> Ctrl+</code> ``) and try:</p>\n<pre><code data-lang=\"bash\">pwd                  # /workspaces/GenAI101 (note: NOT /home/yourname/projects/...)\nls\npython3 --version    # the version the container provides — may differ from your WSL one</code></pre>\n<p>That different working directory and that different Python version are the proof: this is a fresh, isolated environment, not your laptop.</p>\n<h3>Step 4 — Run the curriculum inside its own container</h3>\n<p>While you're in here, you can run the lesson site:</p>\n<pre><code data-lang=\"bash\">python3 -m http.server 8000</code></pre>\n<p>VSCode will pop up a notification offering to open port 8000 in your browser. Click yes — you're now viewing the curriculum being <em>served from inside its own container.</em> (Meta.) This is the workflow you'll use for every real project.</p>\n<h3>Step 5 — Get back out (when you're done)</h3>\n<p>Bottom-left status bar → click the green container indicator → \"Reopen Folder in WSL\". You're back on your normal WSL. The container is still there; it'll be fast next time.</p>\n<h3>Bonus — break it</h3>\n<p>Inside the container terminal, do something dramatic: <code>rm -rf /workspaces/GenAI101/lessons</code> (don't actually run this on your real folder unless you want to test the rebuild!). Then rebuild the container (<code>Ctrl+Shift+P</code> → \"Dev Containers: Rebuild Container\"). The lessons come back, because the lessons live in your WSL folder and the container just mounts them. The <em>environment</em> is the container; your <em>code</em> is your folder. Knowing that split is the actual lesson.</p>\n<h2>Portfolio track</h2>\n<p>Give <code>my-tools</code> its own devcontainer so it runs the same on every machine — yours, a friend's, a fresh laptop next year.</p>\n<pre><code data-lang=\"bash\">cd ~/projects/my-tools\nmkdir -p .devcontainer</code></pre>\n<p>Create <code>.devcontainer/devcontainer.json</code>:</p>\n<pre><code data-lang=\"json\">{\n  \"name\": \"my-tools\",\n  \"image\": \"mcr.microsoft.com/devcontainers/python:3.11\",\n  \"forwardPorts\": [8000]\n}</code></pre>\n<p><code>Ctrl+Shift+P</code> → <strong>Dev Containers: Reopen in Container</strong>. After it builds, open the integrated terminal and run <code>python3 -m http.server 8000</code>. VSCode auto-forwards the port and you see the same site as before — but now anyone who clones this repo gets the same environment, no setup instructions needed.</p>\n<h2>What to take away</h2>\n<ul><li>Containers solve \"works on my machine\" by packaging the whole environment, not just the code.</li><li>Docker is the tool that builds and runs containers. Docker Desktop on Windows + WSL is the install.</li><li>A devcontainer is a container tuned for <em>development</em>, configured via <code>.devcontainer/devcontainer.json</code> in the repo.</li><li>Clone → open in VSCode → \"Reopen in Container\" → you have the same env as the rest of the team. That's the workflow.</li><li>This curriculum has its own devcontainer. Use it. Break it. Rebuild it. That's how it clicks.</li></ul>\n<p>Next: enough Python to read and modify what Claude writes for you.</p>",
  "m06-python-basics": "<h2>Why Python</h2>\n<p>Python is the most-used language in AI, the most-taught language in universities, and one of the most readable languages in existence. If you've ever read pseudocode in a textbook and thought \"that looks reasonable\" — congratulations, you've basically read Python.</p>\n<p>For our stack, Python is the default. Most of the code in our projects is Python: it talks to Claude, processes data, runs little servers, glues APIs together. It's not the only language we touch, but it's the one you'll see most.</p>\n<p>The goal of this lesson isn't to make you a Python developer. It's to make sure that when Claude writes Python for you, <strong>you can read it, follow what it's doing, and ask sharp follow-up questions.</strong> That's the skill.</p>\n<h2>Hello World</h2>\n<p>The first program in any language:</p>\n<pre><code data-lang=\"python\">print(\"Hello, world!\")</code></pre>\n<p>Save that as <code>hello.py</code>, run <code>python hello.py</code> in your terminal, and Python prints it out. That's it. No imports, no boilerplate, no <code>public static void main(String[] args)</code>. Python is designed to get out of your way.</p>\n<h2>Variables</h2>\n<p>No type declarations. You just name a thing and assign it:</p>\n<pre><code data-lang=\"python\">name = \"Ada\"\nage = 36\nis_admin = True</code></pre>\n<p>Python figures out the type from what you assigned. <code>name</code> is a string, <code>age</code> is an integer, <code>is_admin</code> is a boolean. You can change what's in a variable later, no ceremony.</p>\n<h2>Strings and f-strings</h2>\n<p>Strings go in quotes (single or double, your choice). The most-used Python feature for building strings is the <strong>f-string</strong> — you put <code>f</code> in front of the quotes and drop variables in with <code>{}</code>:</p>\n<pre><code data-lang=\"python\">name = \"Ada\"\ngreeting = f\"Hi {name}, welcome to GenAI101\"\nprint(greeting)</code></pre>\n<p>That prints <code>Hi Ada, welcome to GenAI101</code>. You'll use f-strings constantly. Get comfortable with them.</p>\n<h2>Lists and dicts</h2>\n<p>A <strong>list</strong> is an ordered collection. Square brackets:</p>\n<pre><code data-lang=\"python\">people = [\"Ada\", \"Grace\", \"Hopper\"]</code></pre>\n<p>A <strong>dict</strong> (dictionary) is key-value pairs. Curly brackets:</p>\n<pre><code data-lang=\"python\">ages = {\"Ada\": 36, \"Grace\": 12, \"Hopper\": 41}</code></pre>\n<p>You get a value out by its key: <code>ages[\"Grace\"]</code> gives you <code>12</code>. Lists and dicts are the two data structures you'll see in 90% of Python code.</p>\n<h2>if / else</h2>\n<p>Decisions:</p>\n<pre><code data-lang=\"python\">if age &gt;= 18:\n    print(\"Adult\")\nelse:\n    print(\"Kid\")</code></pre>\n<p>Notice the colon and the indentation. Python doesn't use curly braces to group code — it uses indentation. The indented lines are \"inside\" the <code>if</code>. We'll come back to this.</p>\n<h2>for loops</h2>\n<p>Doing something for each item in a collection:</p>\n<pre><code data-lang=\"python\">for person in people:\n    print(f\"Hello, {person}\")</code></pre>\n<p>Read it out loud: \"for each person in people, print hello.\" Python loops read like English on purpose.</p>\n<h2>Functions</h2>\n<p>Reusable blocks of code. Defined with <code>def</code>:</p>\n<pre><code data-lang=\"python\">def greet(name):\n    return f\"Hello, {name}\"\n\nmessage = greet(\"Ada\")\nprint(message)</code></pre>\n<p><code>def</code> says \"I'm defining a function.\" <code>greet</code> is its name. <code>(name)</code> is what it takes in. <code>return</code> is what it gives back. Call it like <code>greet(\"Ada\")</code>, get the result back, do whatever with it.</p>\n<h2>Indentation is syntax, not decoration</h2>\n<p>This is the one Python rule that surprises people from other languages. <strong>The indentation is the grouping.</strong> Most languages use <code>{ }</code> to mark what's inside an <code>if</code> or a function. Python uses whitespace.</p>\n<pre><code data-lang=\"python\">def greet(name):\n    return f\"Hello, {name}\"   # indented = inside the function\n\nprint(\"done\")                  # not indented = outside the function</code></pre>\n<p>Use four spaces per level. VSCode handles this for you automatically. Just don't mix tabs and spaces — Python will yell at you.</p>\n<h2>A tiny end-to-end snippet</h2>\n<p>Here's ten lines that read like real code. A function that greets a list of people, but says something different for kids:</p>\n<pre><code data-lang=\"python\">def greet_everyone(people, ages):\n    for person in people:\n        age = ages[person]\n        if age &lt; 18:\n            print(f\"Hey {person}! How's school?\")\n        else:\n            print(f\"Hi {person}, good to see you.\")\n\npeople = [\"Ada\", \"Grace\", \"Hopper\"]\nages = {\"Ada\": 36, \"Grace\": 12, \"Hopper\": 41}\ngreet_everyone(people, ages)</code></pre>\n<p>Walk through it: a function takes a list and a dict, loops over the list, looks each person up in the dict, branches on their age, and prints something. That's it. You can read every line. You could ask Claude to add a new feature to it — \"also count how many adults there are\" — and you'd be able to spot-check what comes back.</p>\n<blockquote><p>Quick way to remember it: <strong>read Python top to bottom, indent levels tell you what's inside what.</strong></p></blockquote>\n<h2>Lab: write, run, and have your AI explain a Python script</h2>\n<p>10 minutes. The goal: prove to yourself you can write Python, run it, and use your AI helper to deepen understanding of what you wrote.</p>\n<h3>Step 1 — Write the file</h3>\n<p>In a WSL terminal:</p>\n<pre><code data-lang=\"bash\">cd ~/projects\nmkdir -p lab-python\ncd lab-python\ncode .</code></pre>\n<p>In VSCode, create a new file called <code>greet.py</code> and paste in:</p>\n<pre><code data-lang=\"python\">def greet_everyone(people, ages):\n    for person in people:\n        age = ages[person]\n        if age &lt; 18:\n            print(f\"Hey {person}! How's school?\")\n        else:\n            print(f\"Hi {person}, good to see you.\")\n\npeople = [\"Ada\", \"Grace\", \"Hopper\"]\nages = {\"Ada\": 36, \"Grace\": 12, \"Hopper\": 41}\ngreet_everyone(people, ages)</code></pre>\n<p>Save it (<code>Ctrl+S</code>).</p>\n<h3>Step 2 — Run it</h3>\n<p>In VSCode's integrated terminal:</p>\n<pre><code data-lang=\"bash\">python3 greet.py</code></pre>\n<p>You should see three greetings, with Grace getting the kid version.</p>\n<h3>Step 3 — Break it on purpose</h3>\n<p>Comment out the <code>ages = {...}</code> line (put <code>#</code> in front of it). Save. Run again. Read the error message — it should mention <code>NameError: name 'ages' is not defined</code>. Copy that error.</p>\n<h3>Step 4 — Ask your AI helper to walk you through it</h3>\n<p>Open the helper you picked in <a href=\"lessons/02-ai-as-codev.md\" target=\"_blank\" rel=\"noopener\">Module 2</a>. Paste in:</p>\n<blockquote><p><em>\"I'm learning Python. Here's a tiny script and the error I got — explain the error in plain English, what caused it, and how to fix it. Don't rewrite the whole script, just point at the line.\"</em></p>\n<p><em>(paste your script and the error)</em></p></blockquote>\n<p>Notice how specific the answer is when you give it the actual code and the actual error. That's a <em>much</em> better prompt than \"Python NameError what does it mean\".</p>\n<h3>Step 5 — Ask for a small feature</h3>\n<p>Now ask:</p>\n<blockquote><p><em>\"Add a feature to this script that also prints how many adults vs kids there are at the end. Keep it short. Don't restructure anything that doesn't need to change.\"</em></p></blockquote>\n<p>Read what comes back. Look at every line. Does it match what you'd write? If yes, paste it in and run it. If no, push back: <em>\"Why did you do X instead of Y?\"</em> That's the loop you'll be running constantly from now on.</p>\n<h2>Portfolio track</h2>\n<p>Your portfolio is going to have cards — one per tool you build. Hand-editing <code>index.html</code> every time is the kind of friction we automate away. Write your first useful Python script: a tiny builder.</p>\n<p>In <code>~/projects/my-tools/</code>, make a <code>tools/</code> folder and drop a sample card in it:</p>\n<pre><code data-lang=\"bash\">mkdir -p tools</code></pre>\n<p>Save <code>tools/example.md</code>:</p>\n<pre><code>---\nname: Example tool\ndescription: A placeholder card. Replace me when you build Module 10.\nurl: https://example.com\n---</code></pre>\n<p>Now create <code>build.py</code> next to <code>index.html</code>:</p>\n<pre><code data-lang=\"python\">import os, re\n\nCARDS = []\nfor fn in sorted(os.listdir(\"tools\")):\n    if not fn.endswith(\".md\"):\n        continue\n    text = open(f\"tools/{fn}\").read()\n    meta = dict(re.findall(r\"^(\\w+):\\s*(.+)$\", text, re.M))\n    CARDS.append(\n        f'&lt;a class=\"card\" href=\"{meta[\"url\"]}\"&gt;'\n        f'&lt;h2&gt;{meta[\"name\"]}&lt;/h2&gt;&lt;p&gt;{meta[\"description\"]}&lt;/p&gt;&lt;/a&gt;'\n    )\n\nhtml = open(\"index.html\").read()\nhtml = re.sub(\n    r'&lt;div id=\"tools\"&gt;.*?&lt;/div&gt;',\n    f'&lt;div id=\"tools\"&gt;{\"\".join(CARDS)}&lt;/div&gt;',\n    html, flags=re.S,\n)\nopen(\"index.html\", \"w\").write(html)\nprint(f\"Wrote {len(CARDS)} cards.\")</code></pre>\n<p>Run <code>python3 build.py</code>. Refresh the site. Your placeholder card appears. Every future tool you add is just a new <code>tools/*.md</code> and a re-run of <code>build.py</code>.</p>\n<p><strong>Read every line before you commit it.</strong> If anything is unclear, ask Claude to walk you through it. That's the muscle.</p>\n<h2>What to take away</h2>\n<ul><li>Python is readable, dominant in AI, and the default for our stack.</li><li>The basics (variables, f-strings, lists, dicts, if/else, for, def) cover most of what you'll ever read.</li><li>Indentation isn't style. It's syntax. Four spaces, no mixing.</li><li>You don't need to memorize all of Python. You need to be able to <strong>read it, follow it, and ask Claude smart questions about it.</strong> That's the bar.</li></ul>\n<p>Next module: putting it all together — how this stack runs an actual project.</p>",
  "m07-how-nolan-builds": "<h2>The loop is the whole game</h2>\n<p>You've now seen the tools — WSL, the terminal, your editor, the devcontainer, Claude. Tools alone don't ship anything. What ships things is <strong>how you use them, in what order, with what habits.</strong> That's what this lesson is about.</p>\n<p>There's no single right way to build software. The loop below is <strong>our</strong> way. It works for us because it keeps the human (you) in charge of decisions and the AI (Claude) in charge of typing. Steal it, adapt it, eventually replace it with your own — but learn it well first.</p>\n<h2>Get clear before you code</h2>\n<p>The single biggest leverage point in this whole workflow happens <em>before</em> you open Claude. It's the 60 seconds you spend writing down what you actually want.</p>\n<p>Fuzzy goal in, fuzzy output out. Every time. If your ask is \"make the dashboard better,\" you will get something back, you will not like it, and you will not be able to say why. If your ask is \"add a filter at the top of the dashboard that lets the user hide rows older than 30 days, default off, persists in localStorage\" — now Claude has something to actually build, and you have something to actually judge.</p>\n<blockquote><p>If you can't write down in one paragraph what \"done\" looks like, you're not ready to prompt yet. Go think for another minute.</p></blockquote>\n<h2>Brief Claude like a smart teammate who just walked in</h2>\n<p>Imagine a sharp engineer just sat down next to you. They know languages and frameworks deeply, but they know nothing about <em>your</em> project. What do they need from you to be useful in the next ten minutes?</p>\n<ul><li><strong>Goal.</strong> What are we trying to do?</li><li><strong>Context.</strong> What's the relevant file, function, or piece of the system?</li><li><strong>What you've tried.</strong> Saves them from re-walking your dead ends.</li><li><strong>What \"done\" looks like.</strong> What should be true when this works?</li></ul>\n<p>That's also exactly the brief Claude needs. Not more, not less. A good prompt is rarely long — it's <em>specific.</em></p>\n<h2>Tight feedback loops</h2>\n<p>The most common mistake new users make: they prompt Claude, walk away for an hour, come back, and find a sprawling change across twelve files that doesn't quite work. Now they have to either accept a mess they don't understand or throw it all out.</p>\n<p>Don't do that. Work in <strong>small steps:</strong></p>\n<ol><li>Ask for one focused change.</li><li>Run it.</li><li>Look at the real result — the running app, the test output, the actual file.</li><li>Correct.</li><li>Next step.</li></ol>\n<p>Each loop should be minutes, not hours. The shorter your loops, the less wrong Claude can go before you catch it.</p>\n<h2>Review what it writes</h2>\n<p>Code from Claude is code from a teammate. Same standard. You don't merge a teammate's PR without reading it; don't accept Claude's diff without reading it either.</p>\n<p>This is non-negotiable for two reasons. One, Claude is occasionally wrong in ways that look right. Two, the only way you'll actually learn the codebase is by reading the diffs as they happen. The reviewers who skip this step end up six months in not understanding their own project. Don't be that person.</p>\n<p>When something looks off, push back. \"That's not what I meant — the filter needs to persist, not just apply once.\" Claude responds to correction. Use it.</p>\n<h2>Course-correct in place</h2>\n<p>When Claude wanders off — wrong file, wrong assumption, wrong approach — the instinct is often to start a fresh conversation. Resist that. Starting over throws away all the context you just built up.</p>\n<p>Instead, <strong>steer.</strong> Give it the missing piece: \"You're editing the wrong file — the filter logic lives in <code>dashboard/filters.py</code>, not in the component.\" Or: \"Stop. Back up. The user is logged-in only on this route, so we don't need the auth check.\" Claude is built to absorb mid-stream corrections. Use that.</p>\n<h2>Ship small things often</h2>\n<p>A toy that runs end-to-end on day one beats a beautiful architecture diagram that doesn't run on day thirty. Always.</p>\n<p>Look at the projects on Nolan's portfolio — <strong>NoteXT</strong>, <strong>CVstomize</strong>, <strong>HuminLoop</strong>, <strong>TTYL</strong>. None of those started as the finished thing. Every one of them started as a crappy v0 that did one thing badly, and got shaped into the real product through dozens of small loops.</p>\n<p>That's how real software gets made. Not in one heroic sprint. In a thousand small corrections.</p>\n<h2>Exercise: run one full loop on something tiny</h2>\n<p>15 minutes. The point isn't to ship anything serious — it's to <em>feel</em> the loop end-to-end, once, with no stakes.</p>\n<h3>Step 1 — Write the one-paragraph spec</h3>\n<p>In a notes app or a scratch file, write <strong>one paragraph</strong> describing a tiny script you actually want. Examples:</p>\n<ul><li>\"A Python script I run in my terminal that takes a sentence as input and prints back the word count, the average word length, and the longest word.\"</li><li>\"A script that reads a folder of <code>.txt</code> files and prints the filename and first line of each, one per line.\"</li><li>\"A script that opens a URL I paste in, fetches the page title, and prints it.\"</li></ul>\n<p>Don't open your AI helper yet. Get the spec clear first.</p>\n<h3>Step 2 — Hand the spec to your AI helper</h3>\n<p>Open the helper you picked in <a href=\"lessons/02-ai-as-codev.md\" target=\"_blank\" rel=\"noopener\">Module 2</a>. Paste the paragraph and ask:</p>\n<blockquote><p><em>\"Write the smallest single-file Python script that does exactly this. No extras, no error handling I don't need yet. Show me the file and tell me how to run it.\"</em></p></blockquote>\n<h3>Step 3 — Run it</h3>\n<p>Save the file in your <code>~/projects/</code> somewhere. Run it with <code>python3 your_script.py</code>. Did it work?</p>\n<ul><li><strong>If yes:</strong> move to step 4.</li><li><strong>If no:</strong> copy the error, paste it back to the AI with \"this is what happened, fix it.\" Loop until it runs.</li></ul>\n<h3>Step 4 — Use it on a real input</h3>\n<p>Don't stop at \"the parts work.\" Actually feed it real input — a real sentence, a real folder, a real URL. Notice anything awkward.</p>\n<h3>Step 5 — One correction</h3>\n<p>Pick the <strong>one</strong> thing that annoyed you the most. Don't list ten. Pick one. Tell your AI:</p>\n<blockquote><p><em>\"This part is awkward: \\[describe the friction\\]. Fix only that. Don't touch the rest.\"</em></p></blockquote>\n<p>Run it again. Done.</p>\n<p>You just did: <strong>think → prompt → run → look → correct → repeat.</strong> That's the entire workflow this curriculum is built around. Everything else is decoration.</p>\n<h2>Portfolio track</h2>\n<p>You now have a working <code>my-tools</code> site. Apply the loop to <em>itself.</em></p>\n<p>Use the site for two minutes. Resize the browser. Click the placeholder card. Look at the source. Notice everything that annoys you — even tiny things. Then create <code>FRICTION.md</code> at the root of the repo and write one line per annoyance. Don't fix anything yet.</p>\n<p>A starter list to react to (yours will look different):</p>\n<ul><li>Cards look like raw links — no styling at all.</li><li>No way to tell which tools are new vs old.</li><li>I have to remember to run <code>build.py</code> by hand.</li><li>The page title still says \"My Tools\" verbatim — could be my actual name.</li></ul>\n<p>Pick <strong>one</strong> for next session. The smallest, most painful one. That's the loop, applied to the very thing you're building it on.</p>\n<h2>What to take away</h2>\n<p>The loop, named explicitly:</p>\n<p><strong>think → prompt → run → look → correct → repeat</strong></p>\n<p><img src=\"assets/img/build-loop.svg\" alt=\"A circular diagram of the build loop: six nodes — think, prompt, run, look, correct, repeat — each arrow pointing clockwise to the next, closing back to think.\" /></p>\n<ul><li>Think before you prompt. One paragraph of clarity is worth ten of vague intention.</li><li>Brief Claude with goal, context, what you tried, what done looks like.</li><li>Keep loops short. Run it, look at it, correct.</li><li>Read what it writes. Push back when it's wrong.</li><li>Steer mid-stream instead of starting over.</li><li>Ship a small thing today, not a perfect thing eventually.</li></ul>\n<p>That's the whole workflow. Everything else is plumbing.</p>",
  "m08-mcps-and-addons": "<h2>Claude gets more powerful when you give it hands</h2>\n<p>Out of the box, Claude can read your files and run shell commands. That's already a lot. But the real unlock — the thing that turns Claude from \"smart assistant\" into \"co-developer who can actually do stuff in your real tools\" — is <strong>extending it.</strong></p>\n<p>There are four main levers for that: <strong>MCPs, skills, plugins, and hooks.</strong> They sound like jargon. They're not. Let's go through them.</p>\n<p><img src=\"assets/img/mcps-and-addons.svg\" alt=\"Claude Code at the center, with four levers radiating out: MCPs (GitHub, Slack, Filesystem, Postgres, Drive, Canva/Figma), Skills (Daemonstrate, slash commands, your team's conventions), Plugins (TTYL, UI extensions), and Hooks (on-commit, on-stop, on-startup automations).\" /></p>\n<h2>MCP = Model Context Protocol</h2>\n<p>MCP is a standard — invented by Anthropic, now used broadly — for <strong>connecting AI models to external tools.</strong> Think of it as a universal adapter. Any service that exposes an MCP server can be plugged into Claude, and Claude immediately knows how to use it.</p>\n<p>Without MCP, if you wanted Claude to read your GitHub issues, you'd have to copy-paste them in by hand. With the GitHub MCP installed, you say \"look at the open issues on this repo and pick the one I should tackle next\" and Claude just does it.</p>\n<p>Some MCPs you'll run into:</p>\n<ul><li><strong>Filesystem</strong> — read and write files outside your current project.</li><li><strong>GitHub</strong> — issues, PRs, code search, repo metadata.</li><li><strong>Slack</strong> — read channels, post messages, search history.</li><li><strong>Google Drive</strong> — open and reference docs.</li><li><strong>Canva, Figma</strong> — design tools.</li><li><strong>Postgres, SQLite, BigQuery</strong> — databases. Claude can query your data directly.</li><li><strong>Custom MCPs</strong> — anything with an API can be wrapped as one. Nolan's <strong>HuminLoop</strong> is literally an MCP server that pipes screenshot/clipboard context into Claude.</li></ul>\n<blockquote><p>The mental shift: every MCP you add is a new thing Claude can <em>do</em>, not just talk about.</p></blockquote>\n<h2>Claude Code add-ons</h2>\n<p>MCPs are the universal layer. Claude Code itself — the CLI we use — also has its own extension system. Three pieces matter:</p>\n<h3>Skills</h3>\n<p>A <strong>skill</strong> is drop-in domain expertise. It teaches Claude how to do a specific kind of task well, with the right files, conventions, and steps.</p>\n<p>Nolan built and published one called <strong>Daemonstrate.</strong> When loaded, it gives Claude the ability to walk a codebase, identify the architectural scopes, and emit paired <code>.drawio</code> diagrams — one Technical version, one Plain-English version — so a non-coder can understand the system. That's a skill: Claude already knows the languages; the skill teaches it the <em>workflow.</em></p>\n<p>You can write skills for your own team. \"How we structure a PR.\" \"How we write a Postgres migration.\" \"How we name React components.\" Drop the skill in, and every Claude session in that repo follows the conventions.</p>\n<h3>Plugins</h3>\n<p>A <strong>plugin</strong> extends Claude Code itself — its interface, its behavior, what it can show you.</p>\n<p>Nolan's <strong>TTYL</strong> plugin is the example. It's a floating Windows tile that pops up the moment Claude finishes a turn (so you don't have to babysit a long-running task), and it surfaces the prompt-cache TTL so you can see how cache hits are doing. That's not a skill (it's not domain knowledge) and it's not an MCP (it's not an external tool) — it's a modification of Claude Code itself.</p>\n<h3>Slash commands</h3>\n<p>A <strong>slash command</strong> is a tiny saved prompt or workflow you trigger by typing <code>/name</code> in Claude Code. Think of it as a keyboard shortcut for prompts you reuse.</p>\n<p>Examples: <code>/review</code> to kick off a PR review, <code>/init</code> to generate a fresh <code>CLAUDE.md</code> for a repo, <code>/verify</code> to run the app and confirm a change works. You can write your own in minutes. If you find yourself typing the same paragraph of context over and over, that paragraph wants to be a slash command.</p>\n<h3>Hooks</h3>\n<p>A <strong>hook</strong> is an automated behavior Claude Code runs on certain events — when you start a session, when Claude finishes a turn, before a commit, after a file write. Hooks are how you encode \"every time X happens, do Y\" without having to remember.</p>\n<p>Want a desktop notification when Claude is done? Hook on session end. Want every commit auto-linted? Hook on commit. Want the cache warmed when you open a project? Hook on startup.</p>\n<h2>The bigger picture</h2>\n<p>MCPs, skills, plugins, hooks. Four levers. Each one expands what Claude can do <strong>in your real environment</strong>, not in some sandbox.</p>\n<p>This is why a one-person team with a well-tuned Claude setup can ship things that used to take ten people. Not because the AI is magic — because the AI has <em>hands.</em> Every lever you wire up is one more thing the human (you) no longer has to babysit.</p>\n<p>You don't need to install everything on day one. Start with the MCPs you actually use (GitHub and filesystem cover most people), one or two slash commands, and a hook to notify you when long jobs end. Add the rest as friction tells you to.</p>\n<h2>Lab: install Claude Code and wire up one MCP</h2>\n<p>15 minutes. The setup checklist below handles the Claude Code install. This lab puts it to work and installs your first MCP.</p>\n<h3>Step 1 — Install Claude Code (see the checklist)</h3>\n<p>Work through the setup checklist. By the end you should be able to run <code>claude --version</code> in a WSL terminal and get a version number.</p>\n<h3>Step 2 — Use Claude Code on a real folder</h3>\n<p>In your terminal:</p>\n<pre><code data-lang=\"bash\">cd ~/projects/GenAI101    # or any project you have\nclaude</code></pre>\n<p>You're now in Claude Code. Try:</p>\n<pre><code>/init</code></pre>\n<p>This generates a <code>CLAUDE.md</code> for the project — we'll go deep on that in <a href=\"lessons/09-using-ai-well.md\" target=\"_blank\" rel=\"noopener\">Module 9</a>. For now, just watch what it produces. Skim the file. That's persistent memory Claude will read every session.</p>\n<p>Then ask Claude something normal in chat:</p>\n<pre><code>&gt; What does this project do? Read the README and summarize it in 3 sentences.</code></pre>\n<p>Notice: it reads your actual files. That's the difference between Claude Code and a browser chatbot.</p>\n<h3>Step 3 — Install your first MCP</h3>\n<p>We'll add the <strong>GitHub MCP</strong> — most people use this within a day of installing Claude Code. From the Claude Code prompt:</p>\n<pre><code>/mcp</code></pre>\n<p>This opens the MCP management UI. Add the GitHub MCP (it'll walk you through OAuth/login). Once installed, you can ask:</p>\n<pre><code>&gt; List the open issues on my &lt;username&gt;/&lt;repo&gt;.</code></pre>\n<p>Claude now reaches out to GitHub directly. No copy-paste required.</p>\n<blockquote><p>Don't have a GitHub account? Either make one (free at github.com) or pick a different MCP that's relevant to you — Filesystem and Brave Search are easy first ones too.</p></blockquote>\n<h3>Step 4 — Make one slash command</h3>\n<p>Slash commands are the fastest way to feel the leverage. From Claude Code:</p>\n<pre><code>/skill</code></pre>\n<p>Pick the <strong>make a new slash command</strong> option (or look it up in the docs with <code>/help</code> if the UI differs). Make a simple one called <code>/standup</code> whose prompt is:</p>\n<blockquote><p><em>\"Look at git log for the last 24 hours. Summarize what I did in 3 bullet points suitable for a standup.\"</em></p></blockquote>\n<p>Save. Now from any project, you can type <code>/standup</code> and Claude runs the prompt. That's the leverage — every reused prompt becomes one keystroke.</p>\n<h3>Step 5 — Notice what just changed</h3>\n<p>You started this lesson with a chatbot in a tab. You're ending it with an AI that:</p>\n<ul><li>Lives in your terminal, on any project you <code>cd</code> into.</li><li>Has read your code (<code>/init</code> + <code>CLAUDE.md</code>).</li><li>Has hands into GitHub (the MCP).</li><li>Has your own keyboard shortcut (<code>/standup</code>).</li></ul>\n<p>Every lever you added is a thing the AI can now do that it couldn't 15 minutes ago. Add more as friction tells you to.</p>\n<h2>Portfolio track</h2>\n<p>You wired up the GitHub MCP. Use it to put <code>my-tools</code> on the internet.</p>\n<pre><code data-lang=\"bash\">cd ~/projects/my-tools\ngit init\ngit add .\ngit commit -m \"Initial my-tools site\"</code></pre>\n<p>Then in Claude Code:</p>\n<pre><code>&gt; Use the GitHub MCP to create a new public repo called `my-tools` under my account, push this local repo to it, and turn on GitHub Pages on the main branch.</code></pre>\n<p>Read what Claude proposes before approving each step. Within a minute your site is live at <code>https://&lt;your-username&gt;.github.io/my-tools/</code>. Share the URL with one person. Real public artifact, real audience of at least one.</p>\n<h2>What to take away</h2>\n<p>The four levers:</p>\n<ul><li><strong>MCPs</strong> — connect Claude to external tools and services (GitHub, Slack, databases, your own APIs).</li><li><strong>Skills</strong> — teach Claude a specific workflow with the right conventions (like Nolan's <strong>Daemonstrate</strong>).</li><li><strong>Plugins</strong> — extend Claude Code itself (like Nolan's <strong>TTYL</strong>).</li><li><strong>Hooks</strong> — automate behavior on events (turn-end, commit, session-start).</li><li><strong>Slash commands</strong> — saved prompts you trigger with <code>/name</code>.</li></ul>\n<p>Every one you add is leverage. Add them as you need them, not before.</p>",
  "m09-using-ai-well": "<h2>Using AI well (not just using AI)</h2>\n<p>You've got the environment, you've got Claude wired up, and you've seen the loop. Now the question that separates people who get a lot out of AI from people who plateau:</p>\n<p><strong>Are you using it well, or are you just using it?</strong></p>\n<p>Almost everyone using Claude is leaving most of the value on the table. Not because they're doing anything wrong — because they don't know what's possible. This lesson is the part that closes that gap.</p>\n<p>Four ideas. They sound small. Internalize them and your output multiplies.</p>\n<h2>1. AI is your new search engine</h2>\n<p>The first habit to break: reaching for Google.</p>\n<p>When you hit a question — \"how do I do X in Postgres,\" \"what does this error mean,\" \"is there a library for Y\" — the default move is to type it into Google, click through three Stack Overflow tabs, mentally merge the answers, and hope you got the right one for your version.</p>\n<p>That whole workflow is now: <strong>ask Claude.</strong></p>\n<p>Claude has read every Stack Overflow answer, every doc, every blog post. It has <em>also</em> read your code (if you're in Claude Code), so it can answer in terms of your actual project. \"How do I add a Postgres index\" gets you a generic answer from Google. The same question to Claude inside your repo gets you the exact migration file written against your schema, with your project's naming conventions, in your version.</p>\n<p>Search engines give you links. AI gives you answers — tailored to your situation.</p>\n<blockquote><p>The shift: stop typing questions into a search bar. Start typing them into Claude. Especially when you're stuck, confused, or just exploring.</p></blockquote>\n<p>When <em>should</em> you still use Google? When you need a source of truth — official docs you want to read with your own eyes, the GitHub repo of a library, a real-time status page. AI is for understanding; the web is for primary sources you want to verify.</p>\n<h2>2. You can't ask for tools you don't know exist</h2>\n<p>This is the quiet killer. AI lets you build basically anything you can describe — but only if you know what to describe.</p>\n<p>A concrete example: imagine you want to \"make my code look nicer when shared.\" A beginner asks Claude to manually reformat each snippet. Someone who knows the landscape says \"set up Prettier with a <code>.prettierrc</code> and a pre-commit hook.\" Same goal, two completely different requests, because one person knows what tools exist and the other doesn't.</p>\n<p>Multiply that by every dimension of your project. There's probably a:</p>\n<ul><li>standard library for the thing you're hand-rolling</li><li>linter rule that catches the bug you keep making</li><li>CLI tool that does the workflow you've been doing manually</li><li>MCP server (see <a href=\"#\" target=\"_blank\" rel=\"noopener\">Module 8</a>) that gives Claude direct access to the system you're copy-pasting from</li><li>pattern other people have already solved this exact problem with</li></ul>\n<p>You don't know about all of them. Nobody does. The good news: <strong>Claude does.</strong> So the move is to periodically ask:</p>\n<blockquote><p>\"Given what I'm trying to build, are there standard tools, libraries, or patterns I should know about that would make this easier or more idiomatic? What would a senior engineer reach for here?\"</p></blockquote>\n<p>Make this a habit on every new task. Five seconds of \"is there a better way?\" can save you a week of building something that already exists. The biggest accelerator in your toolkit isn't writing code — it's discovering the right tool before you start.</p>\n<h2>3. Two paths, same destination</h2>\n<p>Because AI can build almost anything you describe, you'll end up at the same finished feature from different paths. Some paths are 10× shorter than others. Knowing the difference is most of the skill.</p>\n<p>The two most common traps:</p>\n<p><strong>Trap A: The scenic route.</strong> You ask Claude to build a feature from scratch when a library would have done 80% of it. You end up maintaining 400 lines of custom code instead of importing <code>dateutil</code> and writing 12.</p>\n<p><strong>Trap B: The wrong-shape solution.</strong> You ask for a quick script and Claude gives you a quick script, but the <em>actual</em> shape this problem wanted was a tiny config change in a tool you already use. You now have a script to maintain forever; the right answer was three lines in a YAML file.</p>\n<p>How to dodge both:</p>\n<ul><li>Before you build, ask Claude: <em>\"What are 2–3 different ways to solve this? What are the tradeoffs?\"</em> — and then pick. Don't just accept the first answer.</li><li>When something feels like a lot of work, that's a signal to stop and ask: <em>\"Is there a simpler way? An existing tool? A library? A pattern I'm missing?\"</em></li><li>Periodically zoom out: <em>\"This codebase has feature X. Is the way it's implemented still the right shape, or has the project grown to where a different approach would be cleaner?\"</em></li></ul>\n<p>You won't always take the shortest path. That's fine. But \"I asked, I considered the alternatives, I chose this on purpose\" is a fundamentally different position than \"I built whatever Claude suggested first.\"</p>\n<h2>4. Configure Claude — don't re-explain yourself every session</h2>\n<p>By default, every Claude session starts from zero. It doesn't know your project's conventions, your stack, your style, your goals. If you re-explain that context every time, you're wasting both your time and Claude's attention budget.</p>\n<p>The fix: <strong>persistent instructions.</strong> Claude Code (and the API) reads a few specific files automatically at session start, and you can write things there that Claude will know about every single time.</p>\n<p>The big one is <code>CLAUDE.md</code>. Drop a <code>CLAUDE.md</code> file in your project root, and Claude reads it on every session. Things to put in it:</p>\n<ul><li><strong>What this project is.</strong> A two-sentence description.</li><li><strong>The stack.</strong> Languages, frameworks, key libraries.</li><li><strong>Conventions.</strong> \"We use snake_case for Python files.\" \"All API endpoints live in <code>app/api/</code>.\" \"Don't use <code>requests</code>, use <code>httpx</code>.\"</li><li><strong>Commands.</strong> \"Run tests with <code>pytest -xvs</code>.\" \"Start the dev server with <code>make dev</code>.\"</li><li><strong>What to avoid.</strong> \"Don't add new dependencies without asking.\" \"Don't edit files in <code>legacy/</code>.\"</li><li><strong>Domain knowledge.</strong> \"A 'session' in this codebase means X. A 'workspace' means Y.\"</li></ul>\n<p>You generate the first version by running <code>/init</code> in Claude Code — it'll write a draft for you. Then you edit it.</p>\n<p>You can also have:</p>\n<ul><li><code>CLAUDE.md</code> files <strong>nested per-directory.</strong> A <code>CLAUDE.md</code> inside <code>app/api/</code> applies whenever Claude touches files in that folder. Useful for big repos.</li><li><strong>A user-level <code>CLAUDE.md</code></strong> in <code>~/.claude/CLAUDE.md</code> for your global preferences (\"always run <code>ruff format</code> after editing Python,\" \"prefer functional patterns where they fit\").</li><li><strong>Memory files</strong> that the agent updates itself across sessions — facts about you, your role, recurring feedback, the project. Claude Code maintains these automatically when you say \"remember that…\".</li><li><strong>Skills</strong> (see <a href=\"#\" target=\"_blank\" rel=\"noopener\">Module 8</a>) for whole reusable workflows.</li><li><strong>Slash commands</strong> for prompts you reuse.</li></ul>\n<p>Together these mean: the longer you work on a project, the more Claude <em>knows</em> about how you work, without you having to re-explain. That compounding is real, and it's most of the difference between a Claude session that feels magical and one that feels like teaching a new intern from scratch.</p>\n<blockquote><p>A good <code>CLAUDE.md</code> is one of the highest-leverage 30 minutes you'll ever spend on a project.</p></blockquote>\n<h2>What this looks like as a habit</h2>\n<p>You don't have to do all four every time. But weave them in:</p>\n<ul><li><strong>On every new task:</strong> \"Are there existing tools/libraries/patterns I should know about?\"</li><li><strong>Before building something that feels big:</strong> \"What are 2–3 ways to solve this, with tradeoffs?\"</li><li><strong>Instead of opening Google:</strong> ask Claude.</li><li><strong>On every new project:</strong> write a <code>CLAUDE.md</code> early. Update it as you learn what's annoying you.</li></ul>\n<h2>Lab: build your own CLAUDE.md (and your first habit triggers)</h2>\n<p>15 minutes. By the end you'll have a real <code>CLAUDE.md</code> for a project you care about, plus a one-line cheat sheet of the four habits this lesson teaches.</p>\n<h3>Step 1 — Pick a project (any project)</h3>\n<p>Doesn't have to be code-heavy. Even a notes folder counts. Pick something you actually open more than once a week.</p>\n<pre><code data-lang=\"bash\">cd ~/projects/&lt;your-project&gt;\nclaude</code></pre>\n<h3>Step 2 — Run <code>/init</code></h3>\n<p>In Claude Code:</p>\n<pre><code>/init</code></pre>\n<p>Claude walks the repo, looks at file structure / README / package files, and drafts a <code>CLAUDE.md</code> for you. <strong>Read what it produces.</strong> Most of it will be reasonable; some of it will be guesses.</p>\n<h3>Step 3 — Edit the draft</h3>\n<p>Open the new <code>CLAUDE.md</code> in VSCode and make it actually true. Add or fix:</p>\n<ul><li><strong>What this project is.</strong> Two sentences. What it does and who it's for.</li><li><strong>The stack.</strong> Languages, frameworks, key tools.</li><li><strong>Conventions.</strong> \"We use snake_case.\" \"All API endpoints live in <code>app/api/</code>.\" \"Don't add dependencies without asking.\"</li><li><strong>Commands.</strong> \"Run tests with <code>pytest -xvs</code>.\" \"Start the dev server with <code>make dev</code>.\"</li><li><strong>What to avoid.</strong> Anything Claude could touch and shouldn't.</li></ul>\n<p>Save. Done. Every future Claude session in this project starts knowing all of that.</p>\n<h3>Step 4 — Test it</h3>\n<p>Quit Claude (<code>Ctrl+D</code> or <code>exit</code>). Restart with <code>claude</code>. Ask:</p>\n<pre><code>&gt; What stack does this project use, and how do I run the tests?</code></pre>\n<p>It should answer correctly without you re-explaining. If it doesn't, your <code>CLAUDE.md</code> is missing that info — add it.</p>\n<h3>Step 5 — Write your four-habit cheat sheet</h3>\n<p>In a sticky note (real or digital), write these four lines. Look at them before your next three coding sessions.</p>\n<pre><code>1. Ask Claude before Google.\n2. Periodically: \"is there a better way to do this?\"\n3. Before big work: \"what are 2-3 approaches with tradeoffs?\"\n4. Update CLAUDE.md when something annoys me twice.</code></pre>\n<p>That's the whole lesson, in 4 lines, on a sticky note. Most of the value of this curriculum sits inside those four habits.</p>\n<h2>Portfolio track</h2>\n<p>Drop a <code>CLAUDE.md</code> at the root of <code>my-tools</code> so every future Claude session knows the project. From inside the repo:</p>\n<pre><code data-lang=\"bash\">cd ~/projects/my-tools\nclaude</code></pre>\n<p>Then in Claude Code:</p>\n<pre><code>/init</code></pre>\n<p>Edit the draft so it actually covers:</p>\n<ul><li><strong>What it is.</strong> \"A static portfolio of tiny tools I built for myself, served via GitHub Pages.\"</li><li><strong>Stack.</strong> \"Plain HTML + a single <code>build.py</code> script that regenerates the card grid from <code>tools/*.md</code>.\"</li><li><strong>Convention.</strong> \"Each tool gets one <code>tools/&lt;slug&gt;.md</code> with <code>name:</code>, <code>description:</code>, <code>url:</code> front-matter.\"</li><li><strong>Commands.</strong> \"<code>python3 build.py</code> regenerates <code>index.html</code>. <code>python3 -m http.server 8000</code> previews locally.\"</li><li><strong>What to avoid.</strong> \"Don't edit the <code>&lt;div id=\\\"tools\\\"&gt;</code> block in <code>index.html</code> by hand — re-run <code>build.py</code>.\"</li></ul>\n<p>Save. Now also save a slash command — call it <code>/new-tool</code> — whose prompt is:</p>\n<blockquote><p><em>\"I just finished a tool in another repo. Read that repo's README, then create a <code>tools/&lt;slug&gt;.md</code> here with the right front-matter, run <code>python3 build.py</code>, and stage the changes on a new branch called <code>feat/add-&lt;slug&gt;</code>.\"</em></p></blockquote>\n<p>That single command turns \"I built a thing\" into \"card on the site\" without you typing the steps.</p>\n<h2>What to take away</h2>\n<ul><li><strong>AI is your search engine now.</strong> Ask Claude before you ask Google.</li><li><strong>You can't ask for tools you don't know exist</strong> — so periodically ask Claude \"is there a better way to do this?\" That five-second habit is the single biggest accelerator after the build loop itself.</li><li><strong>The same feature has many implementations.</strong> Some are 10× shorter. Ask for alternatives + tradeoffs before you build.</li><li><strong>Configure Claude with <code>CLAUDE.md</code></strong> so you stop re-explaining yourself. Add skills, slash commands, and memory as they earn their keep.</li></ul>\n<p>Next: pick a small thing and <strong>build your first tool.</strong></p>",
  "m10-build-your-first-tool": "<h2>Now go build something</h2>\n<p>You've read a lot. You've installed a lot. You've watched the workflow described from every angle. None of it will stick until you build something real with it.</p>\n<p>This lesson is the on-ramp to that. By the end you'll have a project idea, a build loop to follow, and no remaining excuse not to start.</p>\n<h2>What makes a good first build</h2>\n<p>Three rules. Break them at your own risk.</p>\n<p><strong>1. Small.</strong> If you can describe it in one paragraph and someone non-technical understands it, the scope is probably right. If you find yourself saying \"and then it also does…\" five times, cut.</p>\n<p><strong>2. Useful to YOU.</strong> Not \"useful to small businesses\" or \"useful to my hypothetical user.\" Useful to <em>you,</em> this week, on a task you actually do. Why? Because you'll use it. And the act of using your own tool every day is what tells you what to fix next. A tool nobody uses dies in a folder.</p>\n<p><strong>3. End-to-end in an afternoon.</strong> v0 should run, badly, the same day you start. Not a polished v1 — a duct-tape v0 that takes input, does the thing, and produces output. Polish comes later, on the back of a working spine.</p>\n<p>What a <strong>bad</strong> first build looks like: \"I'm going to clone Notion.\" \"I'm going to build a social network.\" \"I'm going to build a better ChatGPT.\" These die in week two. Every time. Don't.</p>\n<blockquote><p>A working toy beats an unfinished masterpiece. Every time, in every domain.</p></blockquote>\n<h2>Project ideas</h2>\n<p>Pick one of these or use them as inspiration. All of them are buildable in an afternoon with Claude as your co-developer.</p>\n<ul><li><strong>Daily standup helper.</strong> A CLI you run each morning. It reads your git log from the last 24 hours across a few repos, hands the diff summary to Claude, and gets back a clean \"what I did yesterday\" bullet list ready to paste into Slack.</li></ul>\n<ul><li><strong>Screenshot-to-text clipper.</strong> Hit a shortcut, screenshot a region of the screen, send the image to a vision-capable model, get the extracted text dropped on your clipboard. Two-second tool, used a hundred times a week.</li></ul>\n<ul><li><strong>Scratchpad with auto-tags.</strong> A tiny CLI or web page where you dump quick notes. On save, Claude reads the note, suggests 1–3 tags, and writes the file as Markdown to a notes folder. Later you grep by tag.</li></ul>\n<ul><li><strong>Diagram-to-English companion.</strong> Pairs with Nolan's <strong>Daemonstrate</strong> skill: you point it at a <code>.drawio</code> file and get back a plain-English walkthrough of what the diagram shows. Useful when you want to send a non-technical person the explanation, not the picture.</li></ul>\n<ul><li><strong>Meeting-notes summarizer.</strong> Paste in raw transcript text (from Zoom, Otter, whatever), get back two clean sections: <strong>Decisions</strong> and <strong>Action items (with owners).</strong> Nothing else. The discipline of those two buckets is the value.</li></ul>\n<ul><li><strong>PR-in-plain-English helper.</strong> A CLI you point at a PR URL. It pulls the diff via the GitHub MCP, asks Claude to summarize what changed in plain English, and outputs a 3–5 sentence description suitable for a non-engineer stakeholder.</li></ul>\n<p>Pick the one that, if it existed right now, you'd actually use today.</p>\n<h2>The build loop, in 6 steps</h2>\n<ol><li><strong>Write a one-paragraph spec.</strong> What goes in, what comes out, what \"done\" looks like. Don't open Claude yet.</li><li><strong>Ask Claude to scaffold v1.</strong> Hand it the spec. Ask for the smallest version that runs end-to-end. One file is fine.</li><li><strong>Get it running end-to-end.</strong> Not \"the parts work in isolation.\" Actually run the whole thing on real input. Fix until it does.</li><li><strong>Use it on a real task.</strong> The next time the task comes up in your real workflow, use the tool instead of doing it by hand.</li><li><strong>Note every friction point.</strong> Keep a tiny file open. Every time the tool annoys you, write the friction down in one line. Don't fix yet.</li><li><strong>Fix the top one. Repeat.</strong> Pick the single most painful friction. Fix only that. Use again. New friction list. Loop.</li></ol>\n<p>That loop — used honestly, weekly — is how a v0 becomes a tool people would pay for. It's how <strong>NoteXT</strong> and <strong>HuminLoop</strong> got built. It's how every project on Nolan's portfolio got built.</p>\n<h2>Don't ship what you don't understand</h2>\n<p>One last thing, because it bears repeating: <strong>read what Claude writes.</strong> All of it. Before you run it, before you commit it, before you use it on your real data.</p>\n<p>You will be tempted, especially when the tool is small and you're moving fast, to skim. Don't. The five minutes you spend reading the diff is the five minutes that catches the bug, teaches you the pattern, and earns you the right to actually call this code yours.</p>\n<p>A tool whose source you don't understand is not a tool you built. It's a tool that was assembled near you.</p>\n<h2>Capstone lab: pick one, build the spine today</h2>\n<p>This is the capstone. Don't read past it without doing it.</p>\n<p>You're not going to finish a polished tool in this lab. You're going to get a v0 that <em>runs end-to-end</em> — and you're going to use it on a real task before the day ends.</p>\n<h3>Step 1 — Pick (5 minutes)</h3>\n<p>Look at the project ideas above. Pick <strong>one</strong> that, if it existed right now, you'd use today. Not next week — today. Don't agonize. Pick.</p>\n<h3>Step 2 — Write the one-paragraph spec (5 minutes)</h3>\n<p>Open a scratch file. Write one paragraph that answers:</p>\n<ul><li>What goes in (input)?</li><li>What comes out (output)?</li><li>What does \"this works\" mean?</li></ul>\n<p>Don't open Claude yet. Get the spec clean first. If you can't write the paragraph, your idea isn't clear enough — talk to your AI helper <em>about the idea</em> until it is.</p>\n<h3>Step 3 — Scaffold v0 with Claude Code (15 minutes)</h3>\n<p>In a WSL terminal:</p>\n<pre><code data-lang=\"bash\">cd ~/projects\nmkdir -p my-first-tool\ncd my-first-tool\nclaude</code></pre>\n<p>In Claude Code:</p>\n<pre><code>&gt; Here's the spec: [paste your paragraph]\n&gt; Build the smallest single-file version that takes real input and produces real output. No fancy error handling, no config files, no tests yet. One file. Show me the file and how to run it.</code></pre>\n<p>Read what Claude writes. Don't skim. <strong>You should be able to explain every line out loud.</strong> If you can't, ask Claude to walk you through the parts you don't get before you accept it.</p>\n<h3>Step 4 — Run it end-to-end on real input (10 minutes)</h3>\n<p>Save the file. Run it on a real input — your real notes folder, your real meeting transcript, your real screenshot, whatever your spec called for. Fix until it works.</p>\n<h3>Step 5 — Use it on a real task today (no timer)</h3>\n<p>This is the part most people skip. <strong>Actually use the tool</strong> the next time the task comes up. Don't admire it, don't show it off, just use it.</p>\n<h3>Step 6 — Note one friction (5 minutes)</h3>\n<p>After using it once or twice, open a <code>FRICTION.md</code> next to your script. Write <strong>one line</strong> for each annoying thing. Don't fix anything yet.</p>\n<h3>Step 7 — Fix the worst one (next session)</h3>\n<p>Next session, pick the single most painful friction. Have Claude fix just that. Run again. Repeat.</p>\n<p>That's the loop that turns a v0 toy into a real tool. Use it weekly, on the same tool, for a month. By the end you'll have something that's actually useful — and a feel for how every project on Nolan's portfolio got built.</p>\n<h2>Portfolio track</h2>\n<p>You just built v0 of a real tool. Now ship the card.</p>\n<p>In your <code>my-tools</code> repo:</p>\n<pre><code data-lang=\"bash\">cd ~/projects/my-tools\ngit checkout -b feat/add-&lt;your-tool-slug&gt;</code></pre>\n<p>Either write the card by hand — a new <code>tools/&lt;your-tool-slug&gt;.md</code> with <code>name:</code>, <code>description:</code>, <code>url:</code> front-matter — or in Claude Code:</p>\n<pre><code>/new-tool</code></pre>\n<p>(That's the slash command you saved in Module 9.) Either way, you should end up with a fresh <code>tools/&lt;your-tool-slug&gt;.md</code>, a regenerated <code>index.html</code>, and a branch ready to push.</p>\n<p>Don't merge to main yet. We'll do that, properly, in the next lesson.</p>\n<h2>What to take away</h2>\n<ul><li>Small, useful-to-you, end-to-end in an afternoon. Those are the rules.</li><li>Pick a real task you do regularly. Build the tool that removes the boring part.</li><li>Follow the loop: spec → scaffold → run → use → note friction → fix the worst one → repeat.</li><li>Read every line Claude writes before you ship it.</li></ul>\n<p>You've now got everything you need. The terminal, the devcontainer, Claude, the loop, the workflow, the project. The only thing left is to pick something and start.</p>\n<blockquote><p>If you can think it, you can build it. Now go think of one.</p></blockquote>",
  "m11-collaborative-git": "<h2>Git is how teams ship — including a team of one with an AI</h2>\n<p>Up to now you've been moving fast. Files on your laptop. Edits in your editor. A tool that runs. That's enough to build <em>something</em> — it is not enough to <strong>ship</strong> anything, share anything, work with anyone (Claude included), or recover when things go sideways.</p>\n<p>The bridge from \"I have files\" to \"we have a project\" is <strong>git.</strong></p>\n<p>Git is two things at once, and people learn it badly because they only see one of them at a time:</p>\n<ol><li><strong>A time machine for your code.</strong> Every commit is a saved point you can return to. Mess up? Roll back. Forgot what changed last Tuesday? Look at the diff. That's the <em>version control</em> half.</li><li><strong>A protocol for collaboration.</strong> Branches, pull requests, reviews, merges — that's how two humans (or one human and one AI) can work on the same codebase without trampling each other. That's the half that matters more, and the one this lesson is about.</li></ol>\n<p>You're going to learn <strong>the collaborative flow</strong> here, not the encyclopedia of git commands. Encyclopedias are for reference. The flow is for muscle memory. You'll use it every day, on every project, with every teammate — and yes, <strong>Claude counts as a teammate.</strong> Every diff Claude writes goes through this same loop. Skip the loop and you're shipping code you didn't review, on a branch you don't control, into a history you can't read. That's how teams (and solo builders) ship bugs into production and lose work.</p>\n<h2>The mental model — five words</h2>\n<p>These five words are the whole vocabulary you need on day one. Memorize them.</p>\n<ul><li><strong>Repo</strong> — a project tracked by git. There's one on your laptop (local) and usually one on GitHub (remote). They're copies that talk to each other.</li><li><strong>Commit</strong> — a saved snapshot of your changes, with a message describing why. Commits are immutable. You make many per day.</li><li><strong>Branch</strong> — a parallel line of commits. <code>main</code> is the trunk. Your feature lives on its own branch off main until it's ready to merge back.</li><li><strong>Push / pull</strong> — <code>push</code> sends your local commits to the remote. <code>pull</code> brings the remote's new commits down to your local. That's it.</li><li><strong>PR (pull request)</strong> — a proposal on GitHub: \"here are the commits on my branch, please review and merge them into main.\" Reviews, comments, CI checks, and the merge button all live on the PR.</li></ul>\n<p>Everything else (rebases, cherry-picks, stashes, reflogs) is intermediate. You don't need it to be collaborative-effective today. You need these five.</p>\n<p><img src=\"assets/img/collaborative-git.svg\" alt=\"A diagram of the collaborative git loop: local repo (commits on a feature branch) connects to a remote repo on GitHub via push/pull; a pull request flows from the feature branch into main after review.\" /></p>\n<h2>The one rule that prevents 90% of disasters</h2>\n<blockquote><p><strong>Never commit directly to <code>main</code>. Always work on a branch. Always merge via a pull request.</strong></p></blockquote>\n<p>That rule is not bureaucracy. It is the rule that:</p>\n<ul><li>Lets a teammate (or future-you) see what changed and why, in one reviewable chunk.</li><li>Gives CI a chance to run tests before code lands on the trunk everyone else builds on.</li><li>Keeps <code>main</code> deployable at all times — so when something is on fire, you can deploy <code>main</code> and trust it.</li><li>Lets you abandon a half-baked idea cheaply (delete the branch) instead of having to un-do commits on the trunk.</li></ul>\n<p>Even solo, even with Claude as your only collaborator, <strong>branch-then-PR.</strong> The PR is for you. It's where you read the diff, run the tests, sleep on it if you need to, and only then merge.</p>\n<h2>The collaborative loop, step by step</h2>\n<p>This is the flow. Burn it in. Every day, every project, every feature.</p>\n<pre><code>1. pull         — sync your local main with the remote\n2. branch       — make a new branch off main for your work\n3. work + commit — small focused commits as you go\n4. push         — send your branch up to the remote\n5. open PR      — propose merging your branch into main\n6. review       — read the diff (yours or a teammate's), discuss, fix\n7. merge        — once green and approved, merge to main\n8. clean up     — delete the branch, pull main, you're back at step 1</code></pre>\n<p>Let's walk each step with the commands and the <em>why</em> behind it.</p>\n<h3>1. Pull main first, always</h3>\n<pre><code data-lang=\"bash\">git checkout main\ngit pull</code></pre>\n<p>You start every new piece of work from an up-to-date <code>main</code>. If you skip this step, you'll branch off stale code, hit conflicts later, and waste an hour reconciling history that didn't need reconciling. Two seconds of <code>pull</code> saves an hour of mess.</p>\n<h3>2. Make a branch — and name it like an adult</h3>\n<pre><code data-lang=\"bash\">git checkout -b feat/dashboard-date-filter</code></pre>\n<p>A few branch-naming conventions that pay off forever:</p>\n<ul><li><strong>Prefix by intent.</strong> <code>feat/</code>, <code>fix/</code>, <code>chore/</code>, <code>docs/</code>. Anyone glancing at your branch list knows what's there.</li><li><strong>Short and specific.</strong> <code>feat/dashboard-date-filter</code> is good. <code>feat/stuff</code> is not. <code>feat/refactor-everything-in-the-api-layer-finally</code> is also not.</li><li><strong>Dashes, not spaces or slashes-within-words.</strong> Tools chew on these later. Be kind to them.</li></ul>\n<p>One branch = one focused change. If you find yourself adding three unrelated things to one branch, <strong>stop and split it.</strong> Future-you reviewing the PR will thank present-you.</p>\n<h3>3. Work in small commits</h3>\n<pre><code data-lang=\"bash\"># edit files...\ngit status                 # see what's changed\ngit diff                   # see the actual changes\ngit add path/to/file       # stage what you want in THIS commit\ngit commit -m \"Add date filter to dashboard top bar\"</code></pre>\n<p>Three things matter here:</p>\n<p><strong>Stage deliberately.</strong> <code>git add .</code> is convenient and dangerous — it sweeps up everything in your working directory, including the <code>.env</code> you forgot, the random debug script, the screenshot you left in the folder. Add files by name, or use <code>git add -p</code> to stage hunks interactively. Be picky about what enters history; history is forever.</p>\n<p><strong>Commit small and often.</strong> A good commit is a single logical change you could describe in one sentence. \"Add date filter UI.\" \"Wire up filter to the data layer.\" \"Persist filter selection in localStorage.\" Three commits, one feature, a reviewable story. Not: \"Did a bunch of stuff today\" (one commit, 47 files, nobody can review it).</p>\n<p><strong>Write a real message.</strong> The first line is the <em>what</em> in 50 chars or less, imperative mood (\"Add\", \"Fix\", \"Remove\" — not \"Added\"). If the <em>why</em> isn't obvious, add a blank line and a paragraph. The diff already shows <em>what</em>. Your message should explain <em>why</em> — that's what saves the next person reading your code six months from now.</p>\n<h3>4. Push your branch</h3>\n<pre><code data-lang=\"bash\">git push -u origin feat/dashboard-date-filter</code></pre>\n<p>The <code>-u</code> flag (only needed the first time you push a branch) tells your local branch to \"track\" the remote — after that, plain <code>git push</code> and <code>git pull</code> know which remote branch to talk to.</p>\n<p>Push early. Push often. A pushed branch is a backup. An unpushed branch is one spilled coffee away from being gone.</p>\n<h3>5. Open a pull request</h3>\n<p>On GitHub: branch dropdown → \"Compare &amp; pull request\" → write a real description.</p>\n<p>A good PR description has three things:</p>\n<ul><li><strong>What changed.</strong> In two or three sentences. Not a play-by-play of every commit.</li><li><strong>Why.</strong> The motivation, the user-visible behavior, the bug being fixed.</li><li><strong>How to verify.</strong> A short list of what to click/run to know it works. (\"Open <code>/dashboard</code>, click the date filter, confirm rows older than 30 days disappear.\")</li></ul>\n<p>Keep PRs <strong>small.</strong> A PR that touches 5 files in focused ways gets read carefully and merged quickly. A PR that touches 60 files gets a rubber-stamp \"LGTM\" from a reviewer who skimmed it and missed the bug on line 1,400. Small PRs = real reviews = fewer bugs in main.</p>\n<h3>6. Review — including your own PR before anyone else sees it</h3>\n<p>The first reviewer of every PR is <strong>you.</strong> Before you ask anyone (human or AI) to look, open the diff on GitHub and read it line by line. You'll catch:</p>\n<ul><li>Debug <code>print()</code> / <code>console.log</code> you forgot to remove.</li><li>A <code>TODO</code> that's not actually done.</li><li>A test file you accidentally checked in.</li><li>That <code>.env</code> you almost just shipped to the world.</li></ul>\n<p>Then ask a teammate (or Claude — <code>/review</code>). Take feedback well. The point of a code review is the code, not the coder. If a reviewer is wrong, push back with reasons. If they're right, fix it on the same branch and push — the PR updates automatically. Don't argue for an hour over what would take 90 seconds to change.</p>\n<h3>7. Merge — once it's green and approved</h3>\n<p>Two boxes have to be checked on every merge:</p>\n<ul><li><strong>CI is green.</strong> Tests pass, linters pass, build succeeds. If CI is red, fix it before merging. \"I'll fix it after\" is how <code>main</code> rots.</li><li><strong>At least one approving review.</strong> Even on a solo project, that approver is you-an-hour-later. Sleep on big changes. Re-read the diff in the morning. Then merge.</li></ul>\n<p>Use <strong>\"Squash and merge\"</strong> for feature branches by default — it collapses your branch's commits into one clean commit on main, with the PR title as the message. Cleaner history, easier rollbacks. (Some teams prefer rebase-merge or merge-commits. Whatever your team uses, use it consistently.)</p>\n<h3>8. Clean up</h3>\n<pre><code data-lang=\"bash\">git checkout main\ngit pull\ngit branch -d feat/dashboard-date-filter   # delete the local branch</code></pre>\n<p>GitHub usually offers a \"Delete branch\" button after merge — click it. Stale branches accumulate; clean as you go.</p>\n<p>You are now back at step 1, on a fresh <code>main</code>, ready for the next thing.</p>\n<h2>Working with Claude on the loop</h2>\n<p>Claude can do almost every step of this loop for you — but <strong>only the step.</strong> You still own the decisions and the review.</p>\n<p>What Claude is great at:</p>\n<ul><li><strong>Drafting commit messages.</strong> Stage your changes, run <code>claude</code> in the repo, say \"write a commit message for what's staged.\" Better than 90% of human commit messages on the first try.</li><li><strong>Writing PR descriptions.</strong> Same idea, against the branch diff. Ask for \"What / Why / How to verify.\"</li><li><strong>Reviewing diffs.</strong> Slash command <code>/review</code> opens a proper review of your current branch. Use it before you ask a human.</li><li><strong>Resolving simple merge conflicts.</strong> \"There's a conflict in <code>dashboard.py</code>. Here's mine, here's theirs, here's the intent. Pick the right resolution and explain why.\"</li><li><strong>Investigating \"what changed.\"</strong> <code>git log</code>, <code>git blame</code>, \"why was this line added?\" — Claude is good at these archaeological questions.</li></ul>\n<p>What Claude is <strong>not</strong> allowed to do unsupervised:</p>\n<ul><li><strong>Force-push.</strong> Ever. To anything. Especially <code>main</code>.</li><li><strong>Skip git hooks</strong> (<code>--no-verify</code>). Hooks are there to catch real problems before they land. Bypassing them is how a broken commit lands on main.</li><li><strong>Commit secrets.</strong> If Claude generates a config with a real key in it, that's on you to catch in review.</li><li><strong>Merge its own PR.</strong> Even if Claude opened it. You merge. You're the last set of eyes.</li></ul>\n<p>The mental rule: <strong>let Claude type the commands, but you press the merge button.</strong> It's the same rule as the build loop — Claude does the typing, you do the judging.</p>\n<h2>Merge conflicts — they're not scary, they're just a question</h2>\n<p>A merge conflict happens when two branches changed the same lines and git can't auto-pick. You'll see something like:</p>\n<pre><code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD\nyour version\n=======\ntheir version\n&gt;&gt;&gt;&gt;&gt;&gt;&gt; origin/main</code></pre>\n<p>This is not git breaking. This is git <strong>asking you a question:</strong> \"you changed line 42 to X, they changed it to Y, which one do I keep?\" Nobody but a human (or a human + Claude) can answer that, because the answer depends on what the code is supposed to do.</p>\n<p>The fix:</p>\n<ol><li>Open the file. You'll see the <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> / <code>=======</code> / <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> markers.</li><li>Edit the file so it's the version you actually want. Delete all three marker lines.</li><li><code>git add</code> the file. Repeat for any other conflicted files.</li><li><code>git commit</code> (the merge commit will use a default message — usually fine) or <code>git rebase --continue</code> if you were rebasing.</li></ol>\n<p>If you don't know which version is right, <strong>ask.</strong> Ask the teammate whose code conflicts with yours. Ask Claude to walk through both sides. Don't guess. A conflict resolved wrong is a silent bug that ships.</p>\n<h2>The cardinal sins (do not commit these — pun intended)</h2>\n<p>A short list of things that ruin days, learned the hard way by everyone who's been doing this for a while:</p>\n<ol><li><strong>Committing secrets.</strong> API keys, tokens, <code>.env</code> files, private SSH keys. If it ends up in git history, <strong>rotate the secret immediately</strong> — even if you delete the file in the next commit, the key is forever in the history (and on GitHub's servers, possibly cached by bots that scan for exactly this). The fix is rotation, not deletion. Prevention: a real <code>.gitignore</code> from day one.</li><li><strong>Force-pushing shared branches.</strong> <code>git push --force</code> rewrites history. If you do it on <code>main</code> (or any branch a teammate is also working on), their work disappears or gets corrupted. Only force-push to <em>your own</em> feature branches, and only if nobody else has pulled them. If your team wants safer overwrites, use <code>--force-with-lease</code> instead.</li><li><strong>Working directly on main.</strong> Already covered. Easy rule, big payoff. Branch first, every time.</li><li><strong>Giant PRs.</strong> A PR that touches 40 files won't get a real review. Split it.</li><li><strong>Skipping the diff read.</strong> \"Tests pass, ship it.\" No. Read the diff. Every line. Especially when Claude wrote it. Tests confirm behavior; the diff confirms <em>intent</em> — and the only way to know intent is right is to read it.</li><li><strong>Committing on the wrong branch.</strong> Easy to do when you forget to <code>git checkout</code>. Always check <code>git status</code> (which prints the current branch) before you start typing. If you've already committed to the wrong branch, <code>git log</code> to grab the commit hashes, <code>git reset</code> to undo, <code>git checkout</code> to the right branch, then re-apply (cherry-pick) the commits.</li></ol>\n<h2>The <code>.gitignore</code> you want on day one</h2>\n<p>A <code>.gitignore</code> is a file in your repo that lists patterns git should never track. Have one from commit zero. A reasonable starting point:</p>\n<pre><code># secrets / config\n.env\n.env.*\n*.pem\n*.key\n\n# editor\n.vscode/\n.idea/\n*.swp\n\n# language deps / build output\nnode_modules/\n__pycache__/\n*.pyc\ndist/\nbuild/\n\n# os junk\n.DS_Store\nThumbs.db</code></pre>\n<p>Adjust for your stack. The point is: by the time you <code>git add .</code> for the first commit, anything sensitive or generated is already excluded.</p>\n<h2>Lab: run the full loop on a real branch</h2>\n<p>20 minutes. You'll do the whole flow once, end to end, on a real repo, with a real PR.</p>\n<p>If you don't have a GitHub project of your own yet, use the <code>GenAI101</code> repo (or a fork of it) you cloned in <a href=\"lessons/05-docker-devcontainers.md\" target=\"_blank\" rel=\"noopener\">Module 5</a>. You can also <code>git init</code> a brand-new repo with a <code>hello.md</code> file in 30 seconds and use that.</p>\n<h3>Step 1 — Confirm git is wired up</h3>\n<p>In a WSL terminal:</p>\n<pre><code data-lang=\"bash\">git --version\ngit config --global user.name \"Your Name\"\ngit config --global user.email \"you@example.com\"</code></pre>\n<p>The <code>user.name</code> and <code>user.email</code> are what shows up on every commit you make from now on. Use a real name and the email associated with your GitHub account.</p>\n<h3>Step 2 — Pull main, branch off</h3>\n<pre><code data-lang=\"bash\">cd ~/projects/&lt;your-repo&gt;\ngit checkout main\ngit pull\ngit checkout -b chore/add-my-notes</code></pre>\n<p>You're now on a brand-new branch. <code>git status</code> should print <code>On branch chore/add-my-notes</code>.</p>\n<h3>Step 3 — Make a tiny, focused change</h3>\n<p>Create or edit a file. Anything small — a <code>NOTES.md</code> with your name and date, a typo fix in the README, whatever. Then:</p>\n<pre><code data-lang=\"bash\">git status\ngit diff\ngit add NOTES.md           # name files explicitly; avoid `git add .` while you're learning\ngit commit -m \"Add personal notes file\"</code></pre>\n<p>Read what <code>git status</code> and <code>git diff</code> show you each time. That habit alone separates people who control git from people who get controlled by it.</p>\n<h3>Step 4 — Push the branch</h3>\n<pre><code data-lang=\"bash\">git push -u origin chore/add-my-notes</code></pre>\n<p>GitHub prints a URL in the response — usually a \"compare &amp; pull request\" link. Open it in your browser.</p>\n<h3>Step 5 — Open a PR</h3>\n<p>Fill in:</p>\n<ul><li><strong>Title:</strong> match your commit message intent — short, imperative. \"Add personal notes file.\"</li><li><strong>Body:</strong> three sections. <em>What changed.</em> <em>Why.</em> <em>How to verify.</em> Even on a tiny change, write all three. Get the muscle now.</li></ul>\n<p>Open the PR.</p>\n<h3>Step 6 — Self-review</h3>\n<p>Click the <strong>Files changed</strong> tab. Read your own diff line by line. Is there anything you'd flag if a teammate had written it? Fix anything you flag — commit the fix on the same branch, push, watch the PR update.</p>\n<h3>Step 7 — Ask Claude to review</h3>\n<p>In the repo, in Claude Code:</p>\n<pre><code>/review</code></pre>\n<p>Read what it flags. Address what's worth addressing. Push back on what isn't (it's not always right). Push any fixes.</p>\n<h3>Step 8 — Merge and clean up</h3>\n<p>Once you're happy: <strong>Squash and merge</strong> on GitHub. Then locally:</p>\n<pre><code data-lang=\"bash\">git checkout main\ngit pull\ngit branch -d chore/add-my-notes</code></pre>\n<p>You're back on a clean <code>main</code> that includes your change. That's one full loop. Every feature you ever ship will follow this exact shape.</p>\n<h3>Step 9 — Bonus: cause and resolve a conflict (5 min, optional but recommended)</h3>\n<p>This is the move that flips merge conflicts from \"scary\" to \"routine.\"</p>\n<ol><li>On <code>main</code>, edit a file (say <code>NOTES.md</code>), change line 1, commit, push.</li><li>Make a new branch from the <em>previous</em> state: <code>git checkout -b conflict-demo HEAD~1</code>. Change line 1 differently, commit, push.</li><li>Open a PR for <code>conflict-demo</code>. GitHub will tell you it conflicts.</li><li>Pull main into your branch: <code>git pull origin main</code> — you'll see the conflict markers.</li><li>Resolve by editing the file (pick one version, delete the markers), <code>git add</code>, <code>git commit</code>, <code>git push</code>.</li><li>PR is now mergeable. Merge it.</li></ol>\n<p>Conflicts will never spook you again.</p>\n<h2>Portfolio track</h2>\n<p>You have a branch on <code>my-tools</code> with your first tool card. Run the full loop on it.</p>\n<ol><li><code>git push -u origin feat/add-&lt;your-tool-slug&gt;</code></li><li>Open the PR on GitHub. Write a real <strong>What / Why / How to verify</strong> body.</li><li>Self-review: open the <strong>Files changed</strong> tab. Read every line.</li><li><code>claude</code> → <code>/review</code>. Address what's worth addressing, push fixes.</li><li><strong>Squash and merge.</strong> Watch GitHub Pages redeploy. Refresh the live URL — your card is there.</li><li><code>git checkout main &amp;&amp; git pull &amp;&amp; git branch -d feat/add-&lt;your-tool-slug&gt;</code>.</li></ol>\n<p><strong>Bonus — pair up.</strong> Find one other learner. Add each other as collaborators on your <code>my-tools</code> repos. Each of you opens a PR adding a typo fix or a small style tweak on the <em>other's</em> site. Review. Merge. That's the full collaborative loop, on a real artifact, with a real teammate.</p>\n<p>You now have:</p>\n<ul><li>A public site at <code>https://&lt;your-username&gt;.github.io/my-tools/</code>.</li><li>At least one real tool card on it.</li><li>The build loop <em>and</em> the git loop, both rehearsed on the same artifact.</li></ul>\n<p>Every tool you build for yourself from here on lands here, one PR at a time. The site grows with you.</p>\n<h2>What to take away</h2>\n<ul><li><strong>Git is the collaboration protocol</strong>, not just a backup tool. Even solo, even with Claude as the only teammate, you use it the same way.</li><li><strong>The five-word vocabulary:</strong> repo, commit, branch, push/pull, PR. That's the entire mental model.</li><li><strong>The one rule:</strong> never commit to main. Branch, PR, review, merge. Every time.</li><li><strong>The loop:</strong> pull → branch → commit → push → PR → review → merge → clean up. Run it weekly until it's muscle memory.</li><li><strong>Claude can type every git command, but you press the merge button.</strong> Read every diff. Always.</li><li><strong>Common sins to avoid:</strong> committing secrets, force-pushing shared branches, working on main, giant PRs, skipping diff reads.</li></ul>\n<p>This is the loop that turns a folder of files into a project, a project into a team's project, and a team's project into something that actually ships. Practice it on small things until it disappears into the background. Then everything else gets easier.</p>",
};
